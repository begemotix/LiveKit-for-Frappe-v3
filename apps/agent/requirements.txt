livekit-agents[mcp,openai,mistralai,silero,turn-detector]==1.5.5
mistralai>=2.4.0,<3.0.0
mcp>=1.0.0
griffe>=0.49
python-json-logger~=2.0.0
python-dotenv~=1.0.0
# Optional local TTS provider. Activated via TTS_PROVIDER=piper. When the
# switch is off the package still installs but no Piper HTTP server is
# spawned and no voice models are downloaded. See src/piper_server.py.
piper-tts[http]>=1.3.0
