---
phase: 04-frappe-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - apps/agent/agent.py
  - apps/agent/src/mcp_errors.py
  - apps/agent/tests/test_mcp_integration.py
  - .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md
autonomous: true
requirements:
  - INTG-05
must_haves:
  truths:
    - "Bei fehlender Berechtigung erhalten Nutzer eine klare Einschraenkungsantwort statt eines Abbruchs."
    - "Bei 403 bleibt die Antwortzeit stabil, weil kein Wiederholungsversuch erfolgt."
  artifacts:
    - path: "apps/agent/src/mcp_errors.py"
      provides: "Permission-Fehlerklassifikation inkl. 403/opaque Marker"
    - path: "apps/agent/agent.py"
      provides: "Graceful error handling im MCP-Toolpfad"
    - path: ".planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md"
      provides: "Operator-ready Konfigurations- und Onboarding-Checkliste"
  key_links:
    - from: "apps/agent/agent.py"
      to: "apps/agent/src/mcp_errors.py"
      via: "is_permission_error(err) before user-facing response"
      pattern: "is_permission_error\\("
---

<objective>
Setze robuste Fehlerbehandlung fuer Permission-Faelle um und liefere den verpflichtenden Operator-Handover fuer die Phase.

Purpose: Nutzer erhalten stabile Antworten trotz Berechtigungsgrenzen, und Operatoren koennen Deployment/Onboarding ohne Interpretationsspielraum uebernehmen.
Output: Fehlerklassifikation, Runtime-Handling in `agent.py`, Tests und `OPERATOR-HANDOVER.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-frappe-integration/04-CONTEXT.md
@.planning/phases/04-frappe-integration/04-RESEARCH.md
@apps/agent/agent.py
@apps/agent/tests/test_mcp_integration.py

<interfaces>
From apps/agent/agent.py logging baseline:
```python
ctx.log_context_fields["correlation_id"] = correlation_id
logger.info(..., extra={"correlation_id": correlation_id})
```

Target helper contract:
```python
def is_permission_error(err: Exception) -> bool: ...
def user_facing_permission_message() -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Permission-Fehlerklassifikation und nutzerfreundliches Mapping implementieren</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-CONTEXT.md
    .planning/phases/04-frappe-integration/04-RESEARCH.md
    apps/agent/agent.py
    apps/agent/tests/test_mcp_integration.py
  </read_first>
  <files>apps/agent/src/mcp_errors.py, apps/agent/agent.py, apps/agent/tests/test_mcp_integration.py</files>
  <acceptance_criteria>
    - `apps/agent/src/mcp_errors.py` exportiert `is_permission_error`.
    - `is_permission_error` erkennt mindestens `403`, `permission denied`, `not permitted`, `insufficient permissions`.
    - `agent.py` verwendet bei Permission-Fehlern exakt die feste Nutzerbotschaft ohne technischen Stacktrace (per D-10): `Darauf habe ich mit meinem Agent-Zugang leider keinen Zugriff.`
    - Es gibt keinen Retry-Pfad fuer erkannte Permission-Fehler (per D-11).
    - Logging bei Permission-Fehlern ist strukturiert mit allen Pflichtfeldern (per D-12): `event`, `correlation_id`, `tool`, `error_class` (z. B. via `extra={...}` oder strukturiertem Logger-Kontext konsistent zum bestehenden JSON-Logging).
  </acceptance_criteria>
  <action>Erstelle `apps/agent/src/mcp_errors.py` mit `is_permission_error(err)` fuer Status-/Message-basierte Klassifikation (`403`, `permission denied`, `not permitted`, `insufficient permissions`). Erweitere `apps/agent/agent.py` im Tool-Fehlerpfad so, dass bei `is_permission_error(err)` die feste Antwort per D-10 ausgegeben wird. Logge mit allen D-12-Feldern, z. B. `logger.warning("mcp_permission_denied", extra={"event": "mcp_permission_denied", "correlation_id": correlation_id, "tool": tool_name, "error_class": type(err).__name__})` (Namen/Werte so waehlen, dass die vier Keys immer gesetzt sind). Kein Retry auf diesem Pfad (per D-11). Ergaenze Tests `test_permission_error_user_friendly_no_retry` und `test_permission_error_logged_with_correlation`, wobei letzterer explizit assertiert, dass ein Log-Record/Call die Felder `event`, `correlation_id`, `tool`, `error_class` enthaelt.</action>
  <verify>
    <automated>uv run pytest apps/agent/tests/test_mcp_integration.py::test_permission_error_user_friendly_no_retry apps/agent/tests/test_mcp_integration.py::test_permission_error_logged_with_correlation -x -q</automated>
  </verify>
  <done>Permission-Grenzen werden stabil, benutzerfreundlich und ohne Wiederholungsversuch behandelt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Operator-Handover fuer Phase 04 finalisieren</name>
  <read_first>
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/phases/04-frappe-integration/04-CONTEXT.md
    apps/agent/.env.example
  </read_first>
  <files>.planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md</files>
  <acceptance_criteria>
    - `OPERATOR-HANDOVER.md` enthaelt exakt die vier Pflichtabschnitte aus Projektregel.
    - `OPERATOR-HANDOVER.md` markiert ungenutzte ENV-Werte mit dem Hinweistext `im aktuellen Architekturstand ungenutzt — siehe Decision D-X`, falls vorhanden.
  </acceptance_criteria>
  <action>Erstelle `.planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md` mit den Pflichtsektionen: `Coolify-ENV-Variablen`, `Konfigurierbare Komponenten`, `Nicht-konfigurierbare Komponenten`, `Handover-Checkliste fuer den Onboarding-Termin`. Dokumentiere nur den MCP-Core-Scope von Phase 4 (Verbindung, Discovery, Permission-Handling) und markiere Prompting-bezogene Variablen/Aspekte als out-of-scope fuer diese Phase mit Verweis auf Phase 5. Verweise auf bindende Decisions aus `.planning/PROJECT.md` und `.planning/STATE.md`.</action>
  <verify>
    <automated>rg -n "^(## Coolify-ENV-Variablen|## Konfigurierbare Komponenten|## Nicht-konfigurierbare Komponenten|## Handover-Checkliste fuer den Onboarding-Termin)$" .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md</automated>
  </verify>
  <done>Die Phase ist mit fokussiertem MCP-Core-Handover uebergabefaehig dokumentiert.</done>
</task>

</tasks>

<verification>
Fuehre `uv run pytest apps/agent/tests/test_mcp_integration.py -x -q` sowie `uv run pytest apps/agent/tests -q` aus; bestaetige gruenen Lauf ohne Retry-Loops bei 403. Vor dem Commit zusaetzlich Anti-Drift-Reality-Check ausfuehren: `ls -la`, `ls -1 | wc -l`, `git ls-files | grep -E "^(HEAD|packed-refs|pre-commit\.sample|ORIG_HEAD|FETCH_HEAD|index)$"`; bei Treffern Commit abbrechen und Ursache beheben.
</verification>

<success_criteria>
- INTG-05 ist erfuellt: Permission-/Opaque-Fehler werden graceful behandelt, geloggt und verursachen keinen Session-Crash.
- Operator-Handover beschreibt den MCP-Core-Betrieb ohne Prompting-Erweiterungen.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frappe-integration/04-03-SUMMARY.md`
</output>
