---
phase: 04-frappe-integration
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - .planning/phases/04-frappe-integration/04-VERIFICATION.md
  - .planning/phases/04-frappe-integration/04-HUMAN-UAT.md
  - .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements:
  - INTG-02
  - INTG-03
  - INTG-04
must_haves:
  truths:
    - "Der Agent startet pro Raum eine eigene MCP-Session und beendet diese sauber am Session-Ende."
    - "Der Agent verbindet sich im Produktionspfad ueber stdio-Sidecar und nutzt dabei ausschliesslich die festen Agent-Credentials aus ENV."
    - "Wenn ein Nutzer eine Frappe-Frage stellt, kommen die verfuegbaren Tools aus Live-Discovery der Session statt aus einer statischen App-Allowlist."
  artifacts:
    - path: ".planning/phases/04-frappe-integration/04-VERIFICATION.md"
      provides: "Gate-Abschluss fuer Wave D und feste Ausfuehrungsreihenfolge D -> A -> B -> E -> C -> F im stdio-Produktivpfad"
    - path: ".planning/phases/04-frappe-integration/04-HUMAN-UAT.md"
      provides: "Abnahmefaehige Gate-Evidenzfelder ohne [pending] fuer G1/G2/G3"
    - path: ".planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md"
      provides: "Operator-Checkliste mit explizitem Wave-D-Blocking"
    - path: ".planning/STATE.md"
      provides: "Aktualisierter GO/NO-GO-Status mit Verweis auf abgeschlossene Gate-Nachweise"
  key_links:
    - from: ".planning/phases/04-frappe-integration/04-VERIFICATION.md"
      to: ".planning/phases/04-frappe-integration/04-HUMAN-UAT.md"
      via: "Gate-IDs G1/G2/G3 und gleiche Evidence-Referenzen"
      pattern: "G1|G2|G3"
    - from: ".planning/phases/04-frappe-integration/04-VERIFICATION.md"
      to: ".planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md"
      via: "identische Wave-Reihenfolge D -> A -> B -> E -> C -> F"
      pattern: "D -> A -> B -> E -> C -> F"
---

<objective>
Schliesse die dokumentarischen UAT-Gates fuer Wave D verbindlich, bevor irgendeine weitere Ausfuehrung (A/B/C/E/F) stattfindet.

Purpose: Ohne sauberen Wave-D-Abschluss bleiben alle nachgelagerten Schritte prozessual ungueltig.
Output: Konsistente Gate-Evidenz in Verification/UAT/Handover/State als Startfreigabe fuer die Folgeplaene.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
**Verbindliche Entscheidung (Gap-Closure, nach Root-Cause):** Phase 4 nutzt fuer Frappe einen **lokalen stdio-MCP-Server** analog zum funktionierenden Cursor-Setup (`npx` / `frappe-mcp-server` / gleiches ENV-Triplet). **HTTP-, SSE- oder Site-Root-`/mcp`-Pfadannahmen** sind fuer dieses Setup **nicht** die Planungs- oder Produktivbasis. LiveKit bleibt im offiziellen MCP-Modell, Anbindung ueber **`MCPServerStdio`** (nicht `MCPServerHTTP`). Begruendung: Cursor spricht real stdio mit dem Sidecar; direkte `/mcp`/`/sse`-Checks gegen die Zielsite liefern hier keinen belastbaren HTTP-MCP-Endpunkt fuer den Agent; LiveKit bietet stdio-MCP offiziell an.

@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-frappe-integration/04-CONTEXT.md
@.planning/phases/04-frappe-integration/04-RESEARCH.md
@.planning/phases/04-frappe-integration/04-VERIFICATION.md
@.planning/phases/04-frappe-integration/04-HUMAN-UAT.md
@.planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-D-Gates in Verification/UAT ohne Pending-Felder abschliessen</name>
  <files>.planning/phases/04-frappe-integration/04-VERIFICATION.md, .planning/phases/04-frappe-integration/04-HUMAN-UAT.md</files>
  <read_first>.planning/phases/04-frappe-integration/04-VERIFICATION.md, .planning/phases/04-frappe-integration/04-HUMAN-UAT.md, .planning/phases/04-frappe-integration/04-CONTEXT.md</read_first>
  <action>In `04-VERIFICATION.md` den Abschnitt `Wave-D Gate Evidence (G1/G2/G3)` auf den finalen Ist-Stand bringen: `selected_transport: stdio-sidecar`, `transport_notes: MCPServerStdio(command="npx", args=["-y","frappe-mcp-server"], env={FRAPPE_URL, FRAPPE_API_KEY, FRAPPE_API_SECRET}) als produktiver Pfad; Gate **G2** formuliert den produktiven MCP-Transport als **stdio via lokalem MCP-Server inkl. Runtime-Nachweis** (nicht als Wahl `/mcp` oder `/sse`). Kein HTTP-Endpoint Agent->MCP; keine lokale Bridge, kein REST-Bypass, keine lokale Tool-Allowlist (per D-05, D-06). In `04-HUMAN-UAT.md` fuer G1/G2/G3 dieselben Kernwerte spiegeln (Owner, Datum, boundary_statement, selected_transport, connectivity_log_ref, environment `target-frappe-prod`, captured_at) und `result: pass` fuer G1/G2/G3 sicherstellen; Live-Nachweise: stdio-Start erfolgreich, Tool-Discovery, Read-only-Call, 403/Permission, stabile Session. Wave-Reihenfolge explizit als `D -> A -> B -> E -> C -> F` stehen lassen (per D-13).</action>
  <verify>
    <automated>rg -n "\\[pending\\]|G1|G2|G3|selected_transport: stdio-sidecar|D -> A -> B -> E -> C -> F" .planning/phases/04-frappe-integration/04-VERIFICATION.md .planning/phases/04-frappe-integration/04-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\[pending\\]" .planning/phases/04-frappe-integration/04-VERIFICATION.md .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` liefert keine Treffer.
    - `rg -n "selected_transport: stdio-sidecar|read_only_expectation_confirmed: true" .planning/phases/04-frappe-integration/04-VERIFICATION.md .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` liefert Treffer in beiden Dateien.
    - `rg -n "frappe.get_doc|frappe.list_docs|frappe.search_link|frappe.get_meta" .planning/phases/04-frappe-integration/04-VERIFICATION.md .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` zeigt das identische Tool-Inventar.
  </acceptance_criteria>
  <done>Die Gate-Sektion ist konsistent und ohne offene Platzhalter fuer G1/G2/G3 dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 2: Operator-Handover und State mit abgeschlossenem Wave-D-Gate synchronisieren</name>
  <files>.planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md, .planning/STATE.md</files>
  <read_first>.planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md, .planning/PROJECT.md, .planning/STATE.md, .planning/phases/04-frappe-integration/04-VERIFICATION.md</read_first>
  <action>Vor Bearbeitung von `OPERATOR-HANDOVER.md` die aktiven Decisions aus `.planning/PROJECT.md` und `.planning/STATE.md` lesen und unveraendert respektieren (insb. stdio-Sidecar als Produktivpfad per D-05, kein HTTP-Endpoint Agent->MCP per D-06, kein REST-Bypass, keine lokale Allowlist, Prompt-Notes erst Phase 5). In `OPERATOR-HANDOVER.md` den Gate-Status auf abgeschlossenes Wave D setzen und explizit den Resume-Gate-Hinweis `approved-wave-d` sowie die Reihenfolge `D -> A -> B -> E -> C -> F` eintragen. In `.planning/STATE.md` den Blocker von "pending Wave D approval" auf "Wave D dokumentiert, Freigabe fuer 04-06 bis 04-10 nach approved-wave-d" aktualisieren.</action>
  <verify>
    <automated>rg -n "Wave D|D -> A -> B -> E -> C -> F|NO-GO|GO" .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "approved-wave-d|D -> A -> B -> E -> C -> F" .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md` liefert Treffer.
    - `rg -n "Wave D|04-06 bis 04-10" .planning/STATE.md` liefert Treffer.
    - `rg -n "stdio|kein HTTP-Endpoint|REST-Fallback|Allowlist|Phase 5" .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md` bestaetigt unveraenderte Scope-Grenzen.
  </acceptance_criteria>
  <done>Handover und State verweisen konsistent auf den abgeschlossenen Gate-Zustand und die freigegebene Folgewelle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Wave-D-Gate-Freigabe bestaetigen</name>
  <files>.planning/phases/04-frappe-integration/04-VERIFICATION.md, .planning/phases/04-frappe-integration/04-HUMAN-UAT.md, .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md</files>
  <read_first>.planning/phases/04-frappe-integration/04-VERIFICATION.md, .planning/phases/04-frappe-integration/04-HUMAN-UAT.md, .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md</read_first>
  <action>Fuehre die manuelle Gate-Abnahme aus und erteile nur dann Freigabe, wenn alle drei Gates G1/G2/G3 jeweils `result: pass` haben, der produktive Transport exakt `stdio-sidecar` ist (**stdio via lokalem MCP-Server**, kein HTTP-Endpoint Agent->MCP, per D-05/D-06) und die Reihenfolge `D -> A -> B -> E -> C -> F` in Verification und Handover identisch dokumentiert ist.</action>
  <what-built>Verbindliche Gate-Dokumentation fuer G1/G2/G3 inklusive expliziter Startfreigabe fuer A/B/C/E/F</what-built>
  <how-to-verify>
    1) `04-VERIFICATION.md` oeffnen und pruefen, dass G1/G2/G3 keine `[pending]`-Evidenz mehr enthalten.
    2) `04-HUMAN-UAT.md` pruefen: gleiche Nachweise wie in Verification, gleiche Endpoint-/Transport-Festlegung.
    3) `OPERATOR-HANDOVER.md` pruefen: Wave-Reihenfolge exakt `D -> A -> B -> E -> C -> F`.
    4) Freigabe nur erteilen, wenn keine verbotene Architektur eingefuehrt wurde.
  </how-to-verify>
  <verify>
    <automated>rg -n "G1|G2|G3|D -> A -> B -> E -> C -> F" .planning/phases/04-frappe-integration/04-VERIFICATION.md .planning/phases/04-frappe-integration/04-HUMAN-UAT.md .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md</automated>
  </verify>
  <acceptance_criteria>
    - Manuelle Freigabe wird nur mit exakt `approved-wave-d` erteilt.
    - `rg -n "selected_transport: stdio-sidecar|result: pass" .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` zeigt pass-Evidenz fuer die Gates.
    - `rg -n "D -> A -> B -> E -> C -> F" .planning/phases/04-frappe-integration/04-VERIFICATION.md .planning/phases/04-frappe-integration/OPERATOR-HANDOVER.md` zeigt identische Reihenfolge.
  </acceptance_criteria>
  <done>Human-Abnahme ist dokumentiert und hat `approved-wave-d` geliefert.</done>
  <resume-signal>Type "approved-wave-d" oder dokumentiere Korrekturen.</resume-signal>
</task>

</tasks>

<verification>
Pruefe Konsistenz ueber alle Gate-Dokumente und bestaetige, dass Wave D als einzige blockierende Startvoraussetzung fuer alle Folgewaves markiert ist.
Vor jedem Commit zwingend Anti-Drift-Reality-Check ausfuehren: `ls -la`, `ls -1 | wc -l`, `git ls-files | grep -E "^(HEAD|packed-refs|pre-commit\.sample|ORIG_HEAD|FETCH_HEAD|index)$"`; bei Treffern Commit abbrechen.
</verification>

<success_criteria>
- Wave D ist als abgeschlossenes, blockierendes Gate nachgewiesen.
- Alle Folgeplaene duerfen nur nach `approved-wave-d` starten.
- Phase-4-Architekturgrenzen bleiben unveraendert.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frappe-integration/04-05-SUMMARY.md`
</output>
