---
phase: 04-frappe-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/agent/agent.py
  - apps/agent/tests/test_mcp_integration.py
autonomous: true
requirements:
  - INTG-03
  - INTG-04
must_haves:
  truths:
    - "Wenn eine Agent-Session beendet wird, bleiben keine offenen MCP-Verbindungen zurueck und die naechste Session startet sauber neu."
    - "Nutzer koennen den Agenten nicht auf andere Credentials umschalten; der Zugriff bleibt auf genau eine Agent-Identitaet beschraenkt."
    - "Bei einer neuen Session sind die verfuegbaren Tools dynamisch vom MCP-Server geladen und keine statische App-Toolliste."
    - "Datenanfragen werden fuer Nutzer ohne direkte Frappe-API-Bypaesse bearbeitet."
  artifacts:
    - path: "apps/agent/agent.py"
      provides: "Session-scoped MCP lifecycle und Discovery-Wiring"
    - path: "apps/agent/tests/test_mcp_integration.py"
      provides: "Schutz gegen Credential-Switch und API-Bypass"
  key_links:
    - from: "apps/agent/agent.py"
      to: "apps/agent/src/frappe_mcp.py"
      via: "build_frappe_mcp_server() in AgentSession setup"
      pattern: "mcp_servers=\\[build_frappe_mcp_server\\(\\)\\]"
    - from: "apps/agent/agent.py"
      to: "apps/agent/tests/test_mcp_integration.py"
      via: "Session-End-Callback ruft MCP cleanup deterministisch auf"
      pattern: "(close|aclose|shutdown)"
    - from: "apps/agent/agent.py"
      to: "apps/agent/tests/test_mcp_integration.py"
      via: "Discovery wird zur Laufzeit aus MCP-Sessionzustand validiert"
      pattern: "(list_tools|tools/list|mcp_tools)"
---

<objective>
Verdrahte den Agent-Lifecycle mit session-lokalem MCP-Server und halte die Architektur auf dedizierte Agent-Credentials plus dynamische Discovery fest.

Purpose: Erfuellt den Kern der Integrationsarchitektur mit minimalem Drift-Risiko gegen MCP-Reinheit.
Output: Session-Wiring in `agent.py` und Regressionstests fuer Identity/Discovery-Grenzen.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-frappe-integration/04-CONTEXT.md
@.planning/phases/04-frappe-integration/04-RESEARCH.md
@.planning/phases/04-frappe-integration/04-01-PLAN.md
@apps/agent/agent.py
@apps/agent/src/frappe_mcp.py
@apps/agent/tests/test_mcp_integration.py

<interfaces>
From apps/agent/agent.py:
```python
session = AgentSession(
    llm=model,
    allow_interruptions=True,
)
```

Required target signature after this plan:
```python
session = AgentSession(
    llm=model,
    allow_interruptions=True,
    mcp_servers=[build_frappe_mcp_server()],
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session-scope MCP-Lifecycle in `agent.py` integrieren</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-CONTEXT.md
    apps/agent/agent.py
    apps/agent/src/frappe_mcp.py
    apps/agent/tests/test_agent.py
  </read_first>
  <files>apps/agent/agent.py</files>
  <acceptance_criteria>
    - `agent.py` importiert `build_frappe_mcp_server` aus `apps/agent/src/frappe_mcp.py`.
    - `AgentSession(...)` enthaelt `mcp_servers=[build_frappe_mcp_server()]` (per D-01, D-02, D-03).
    - Beim Session-Ende wird die session-lokale MCP-Verbindung deterministisch beendet (per D-02).
    - `agent.py` enthaelt keinen Zugriff auf Frontend- oder User-Credential-Variablen fuer MCP (per D-04).
    - Es gibt weiterhin keine direkte Nutzung von `requests`, `httpx` oder `frappe` in `agent.py` (per D-07).
  </acceptance_criteria>
  <action>Erweitere `apps/agent/agent.py` so, dass die Session-Initialisierung exakt `mcp_servers=[build_frappe_mcp_server()]` verwendet (per D-01, D-03). Importiere die Funktion aus dem neuen Modul. Ergaenze einen expliziten Session-End-Hook (z. B. im vorhandenen Lifecycle-Callback) der die MCP-Server-Ressource sauber schliesst (`close`/`aclose`/`shutdown`) und Fehler beim Cleanup strukturiert loggt, statt sie zu ignorieren (per D-02). Entferne keinen bestehenden Participant-Lifecycle (`participant_joined`, `session.start`, `ctx.connect`), sondern verdrahte MCP nur als Session-Ressource. Stelle sicher, dass keine runtime-umschaltbaren Credential-Pfade entstehen (keine alternativen ENV-Namen wie `USER_*`, keine Parameteruebergabe von Frontenddaten).</action>
  <verify>
    <automated>uv run pytest apps/agent/tests/test_mcp_integration.py::test_session_has_mcp_server -x -q</automated>
  </verify>
  <done>AgentSession wird konsistent mit dediziertem MCP-Server pro Session gestartet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard- und Evidenz-Tests fuer Cleanup, Discovery und MCP-Reinheit erweitern</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-VALIDATION.md
    apps/agent/tests/test_mcp_integration.py
    apps/agent/agent.py
  </read_first>
  <files>apps/agent/tests/test_mcp_integration.py</files>
  <acceptance_criteria>
    - Test `test_session_has_mcp_server` validiert, dass `AgentSession` mit genau einem MCP-Server erstellt wird.
    - Test `test_session_end_cleans_up_mcp_server` validiert den deterministischen Cleanup bei Session-Ende (per D-02).
    - Test `test_no_runtime_credential_switch` validiert, dass nur `FRAPPE_API_KEY`/`FRAPPE_API_SECRET` genutzt werden.
    - Test `test_dynamic_tool_discovery_runtime_evidence` liefert explizite Evidenz, dass Toolliste zur Laufzeit aus Discovery stammt statt aus statischer App-Liste (per D-05).
    - Test `test_no_direct_frappe_api_calls` validiert, dass keine direkten HTTP/Frappe-Bypass-Pfade eingefuehrt wurden.
    - `uv run pytest apps/agent/tests/test_mcp_integration.py -x -q` endet mit Exit-Code 0.
  </acceptance_criteria>
  <action>Ergaenze `apps/agent/tests/test_mcp_integration.py` um mindestens vier Tests (per INTG-03, INTG-04): `test_session_has_mcp_server` mit Fake-`AgentSession`, die `mcp_servers`-Argumente erfasst; `test_session_end_cleans_up_mcp_server` mit Fake-MCP-Server inkl. Close-Mock und Assertion auf genau einen Cleanup-Call; `test_no_runtime_credential_switch` mit Assertions, dass Session-Build nur auf `FRAPPE_API_KEY`/`FRAPPE_API_SECRET` basiert; `test_dynamic_tool_discovery_runtime_evidence` mit Nachweis, dass Tools zur Laufzeit aus MCP-Discovery (`list_tools`/Session-Tools) gelesen werden und keine statische App-Allowlist genutzt wird; sowie `test_no_direct_frappe_api_calls` mit source-level Assertion auf `agent.py`, dass keine direkten Frappe-REST-Patterns (`/api/method`, `requests.`, `httpx.`) genutzt werden.</action>
  <verify>
    <automated>uv run pytest apps/agent/tests/test_mcp_integration.py -x -q</automated>
  </verify>
  <done>Die Architekturgrenzen fuer Agent-Identitaet und MCP-only Discovery sind testseitig abgesichert.</done>
</task>

</tasks>

<verification>
Führe `uv run pytest apps/agent/tests/test_mcp_integration.py -x -q` aus und bestaetige, dass Session-Wiring, Identity-Guard und MCP-Reinheit grün sind. Vor dem Commit zusaetzlich Anti-Drift-Reality-Check ausfuehren: `ls -la`, `ls -1 | wc -l`, `git ls-files | grep -E "^(HEAD|packed-refs|pre-commit\.sample|ORIG_HEAD|FETCH_HEAD|index)$"`; bei Treffern Commit abbrechen und Ursache beheben.
</verification>

<success_criteria>
- INTG-03: Agent arbeitet ausschliesslich mit dedizierten Agent-Credentials.
- INTG-04: Discovery bleibt dynamisch ueber MCP; keine direkte Frappe-API-Logik wird eingefuehrt.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frappe-integration/04-02-SUMMARY.md`
</output>
