---
phase: 04-frappe-integration
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - apps/agent/pyproject.toml
  - apps/agent/requirements.txt
  - apps/agent/tests/test_mcp_integration.py
  - .planning/phases/04-frappe-integration/04-HUMAN-UAT.md
autonomous: true
gap_closure: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
  - INTG-04
  - INTG-05
must_haves:
  truths:
    - "Nutzer koennen wieder eine Session starten, ohne dass der Agent vor der ersten Antwort abstuerzt."
    - "Nutzer erhalten auf Frappe-Anfragen wieder Antworten, weil die MCP-Verbindung im Laufzeitpfad verfuegbar ist."
    - "Operatoren koennen Discovery-, Read-only- und 403-UAT wieder am echten Zielsystem durchfuehren."
  artifacts:
    - path: "apps/agent/pyproject.toml"
      provides: "UV-basierter Dependency-Contract mit MCP-Unterstuetzung im LiveKit-Agent."
    - path: "apps/agent/requirements.txt"
      provides: "Pip-basierter Runtime-Contract fuer Container-Start mit MCP-Abhaengigkeiten."
    - path: "apps/agent/tests/test_mcp_integration.py"
      provides: "Regressionstest fuer MCP-Import-/Runtime-Verfuegbarkeit."
    - path: ".planning/phases/04-frappe-integration/04-HUMAN-UAT.md"
      provides: "Aktualisierter UAT-Lauf mit dokumentiertem unblockten Re-Test-Pfad."
  key_links:
    - from: "apps/agent/pyproject.toml"
      to: "apps/agent/agent.py"
      via: "livekit-agents inklusive mcp extra"
      pattern: "livekit-agents\\[.*mcp.*\\]"
    - from: "apps/agent/requirements.txt"
      to: "apps/agent/agent.py"
      via: "Runtime-Image installiert MCP-faehige Agent-Abhaengigkeit"
      pattern: "livekit-agents\\[.*mcp.*\\]"
    - from: "apps/agent/tests/test_mcp_integration.py"
      to: "apps/agent/src/frappe_mcp.py"
      via: "Import-/Factory-Regressionstest gegen fehlende MCP-Module"
      pattern: "build_frappe_mcp_server|livekit\\.agents\\.mcp"
---

<objective>
Schliesse den UAT-Blocker, bei dem der Agent vor Session-Join mit fehlender MCP-Dependency abstuerzt, und stelle eine reproduzierbare Runtime-Verifikation fuer den MCP-Startpfad her.

Purpose: Ohne lauffaehige MCP-Abhaengigkeiten sind alle INTG-Ziele praktisch nicht validierbar, obwohl der Codepfad implementiert ist.
Output: Synchronisierte Dependency-Vertraege fuer `uv` und `pip`, Runtime-Regressionstest, sowie aktualisierte UAT-Dokumentation fuer den Re-Test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frappe-integration/04-CONTEXT.md
@.planning/phases/04-frappe-integration/04-RESEARCH.md
@.planning/phases/04-frappe-integration/04-VERIFICATION.md
@.planning/phases/04-frappe-integration/04-HUMAN-UAT.md
@apps/agent/pyproject.toml
@apps/agent/requirements.txt
@apps/agent/agent.py
@apps/agent/src/frappe_mcp.py
@apps/agent/tests/test_mcp_integration.py

<interfaces>
From apps/agent/src/frappe_mcp.py (per D-05/D-06, stdio-Sidecar):
```python
def build_frappe_mcp_server() -> "mcp.MCPServerStdio":
    ...
```

Required dependency target for runtime:
```text
livekit-agents[mcp,openai,silero,turn-detector]~=1.5
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP-Runtime-Abhaengigkeiten fuer uv und pip synchronisieren</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-HUMAN-UAT.md
    .planning/phases/04-frappe-integration/04-VERIFICATION.md
    .planning/phases/04-frappe-integration/04-CONTEXT.md
    apps/agent/pyproject.toml
    apps/agent/requirements.txt
    apps/agent/agent.py
    apps/agent/src/frappe_mcp.py
  </read_first>
  <files>apps/agent/pyproject.toml, apps/agent/requirements.txt</files>
  <acceptance_criteria>
    - `apps/agent/pyproject.toml` enthaelt in `dependencies` einen Eintrag `livekit-agents[` mit dem Extra `mcp`.
    - `apps/agent/requirements.txt` enthaelt exakt einen `livekit-agents[`-Eintrag mit `mcp` und `openai`.
    - In beiden Dateien steht keine Version `~=1.4.0` mehr fuer `livekit-agents`.
    - `uv run python -c "from livekit.agents import mcp; print('mcp-ok')"` gibt `mcp-ok` aus.
    - `python -c "from livekit.agents import mcp; print('mcp-ok')"` gibt `mcp-ok` aus, nachdem `pip install -r apps/agent/requirements.txt` ausgefuehrt wurde.
  </acceptance_criteria>
  <action>Aktualisiere `apps/agent/pyproject.toml` auf einen konsistenten LiveKit-Dependency-Eintrag mit MCP-Unterstuetzung: ersetze `livekit-agents[silero,turn-detector]~=1.5` durch `livekit-agents[mcp,openai,silero,turn-detector]~=1.5` (MCP-Runtime fuer den stdio-Sidecar-Produktivpfad per D-05/D-06). Aktualisiere `apps/agent/requirements.txt` von `livekit-agents[openai]~=1.4.0` auf `livekit-agents[mcp,openai]~=1.5` und belasse `python-json-logger~=2.0.0` sowie `python-dotenv~=1.0.0` unveraendert. Fuehre danach beide Import-Smoke-Commands aus (uv und python/pip Pfad), damit der Runtime-Import `from livekit.agents import mcp` in beiden Installationswegen explizit nachgewiesen wird.</action>
  <verify>
    <automated>uv run python -c "from livekit.agents import mcp; print('mcp-ok')" && uv run pytest apps/agent/tests/test_mcp_integration.py -x -q</automated>
  </verify>
  <done>Der Agent-Worker kann die MCP-Module in beiden Runtime-Pfaden (uv und pip/requirements) ohne ImportError laden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime-Start-Regressionstest und UAT-Re-Test-Protokoll vervollstaendigen</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-HUMAN-UAT.md
    apps/agent/tests/test_mcp_integration.py
    apps/agent/agent.py
    apps/agent/src/frappe_mcp.py
  </read_first>
  <files>apps/agent/tests/test_mcp_integration.py, .planning/phases/04-frappe-integration/04-HUMAN-UAT.md</files>
  <acceptance_criteria>
    - `apps/agent/tests/test_mcp_integration.py` enthaelt einen Test `test_mcp_module_import_available`.
    - `test_mcp_module_import_available` importiert `livekit.agents.mcp` direkt und scheitert bei fehlender MCP-Dependency.
    - `apps/agent/tests/test_mcp_integration.py` enthaelt einen Test `test_agent_module_import_does_not_raise_mcp_import_error`.
    - `uv run pytest apps/agent/tests/test_mcp_integration.py::test_mcp_module_import_available -x -q` endet mit Exit-Code 0.
    - `.planning/phases/04-frappe-integration/04-HUMAN-UAT.md` dokumentiert ein neues Gap-Update mit Ergebnis der Re-Tests fuer Discovery, E2E-Read-only und 403-Fall.
  </acceptance_criteria>
  <action>Erweitere `apps/agent/tests/test_mcp_integration.py` um zwei gezielte Regressionstests: (1) `test_mcp_module_import_available`, der `from livekit.agents import mcp` ausfuehrt und bei Misserfolg die konkrete Exception meldet; (2) `test_agent_module_import_does_not_raise_mcp_import_error`, der den Importpfad von `apps/agent/agent.py` in isolierter Umgebung prueft und explizit sicherstellt, dass kein `ModuleNotFoundError: mcp` auftritt. Aktualisiere anschliessend `.planning/phases/04-frappe-integration/04-HUMAN-UAT.md` mit einem neuen Zeitstempel-Abschnitt unter `## Gaps`, der den entfernten Blocker dokumentiert und die drei Human-Tests (`Live-MCP Discovery`, `End-to-End Read-Only Datenabfrage`, `403-Rechtefall`) als erneute Pflicht-Reihe auffuehrt; setze Statusfelder auf den tatsaechlichen Re-Test-Stand statt `pending`/`failed` zu belassen.</action>
  <verify>
    <automated>uv run pytest apps/agent/tests/test_mcp_integration.py::test_mcp_module_import_available -x -q && uv run pytest apps/agent/tests/test_mcp_integration.py::test_agent_module_import_does_not_raise_mcp_import_error -x -q</automated>
  </verify>
  <done>Ein automatisierter Regressionstest verhindert erneute MCP-Import-Ausfaelle, und die UAT-Datei beschreibt den unblockten Re-Test-Prozess transparent.</done>
</task>

</tasks>

<verification>
Fuehre `uv run pytest apps/agent/tests/test_mcp_integration.py -x -q` sowie `uv run pytest apps/agent/tests -q` aus. Danach starte den Agenten einmal lokal mit aktivierten FRAPPE-ENV-Werten und bestaetige, dass kein `ModuleNotFoundError: mcp` mehr im Startlog erscheint. Vor Commit Anti-Drift-Reality-Check ausfuehren (`ls -la`, `ls -1 | wc -l`, `git ls-files | grep -E "^(HEAD|packed-refs|pre-commit\\.sample|ORIG_HEAD|FETCH_HEAD|index)$"`).
</verification>

<success_criteria>
- UAT-Blocker "Agent antwortet nicht wegen fehlender MCP-Dependency" ist behoben und per automatischem Importtest abgesichert.
- Phase-04-Tests laufen ohne MCP-ImportError, sodass Discovery/E2E/403-Human-Checks wieder fachlich gueltig durchfuehrbar sind.
- INTG-01 bis INTG-05 bleiben durchfuehrbar und verifizierbar, weil die Runtime nicht mehr vor Session-Start abstuerzt.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frappe-integration/04-04-SUMMARY.md`
</output>
