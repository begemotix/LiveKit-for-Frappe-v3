---
phase: 04-frappe-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/frappe_mcp.py
  - apps/agent/.env.example
  - apps/agent/tests/test_mcp_integration.py
autonomous: true
requirements:
  - INTG-01
  - INTG-02
must_haves:
  truths:
    - "Beim Start mit gueltiger Konfiguration kann der Agent MCP-Tools ohne Auth-Fehler nutzen."
    - "Wenn Credentials fehlen, meldet der Agent klar, welche MCP-Umgebungswerte fehlen."
    - "Der Agent nutzt fuer MCP-Aufrufe ausschliesslich die dedizierten Agent-Credentials aus ENV."
  artifacts:
    - path: "apps/agent/src/frappe_mcp.py"
      provides: "MCP-Factory fuer URL und Auth-Header"
    - path: "apps/agent/.env.example"
      provides: "Verbindlicher ENV-Vertrag fuer MCP-Verbindung"
    - path: "apps/agent/tests/test_mcp_integration.py"
      provides: "Automatisierte Basisabdeckung fuer MCP-Konfiguration"
  key_links:
    - from: "apps/agent/.env.example"
      to: "apps/agent/src/frappe_mcp.py"
      via: "FRAPPE_MCP_URL, FRAPPE_API_KEY, FRAPPE_API_SECRET"
      pattern: "os\\.getenv\\(\"FRAPPE_(MCP_URL|API_KEY|API_SECRET)\"\\)"
---

<objective>
Lege die technische Grundlage fuer die MCP-Anbindung des Agenten fest, damit Session-Integration in den Folgeplaenen deterministisch umgesetzt werden kann.

Purpose: Verbindlicher ENV- und Factory-Vertrag reduziert Implementierungsrisiko fuer Auth und Connection-Lifecycle.
Output: MCP-Factory-Modul, ENV-Schluessel in `.env.example`, und MCP-Basistests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frappe-integration/04-CONTEXT.md
@.planning/phases/04-frappe-integration/04-RESEARCH.md
@apps/agent/agent.py
@apps/agent/.env.example

<interfaces>
From apps/agent/agent.py:
```python
def load_agent_instructions():
    ...

async def entrypoint(ctx: JobContext):
    ...
```

From livekit MCP integration contract (as used in project research):
```python
mcp.MCPServerHTTP(
    "https://frappe.example.com/mcp",
    headers={"Authorization": f"token {api_key}:{api_secret}"},
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP-Factory mit festen ENV-Credentials implementieren</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-CONTEXT.md
    .planning/phases/04-frappe-integration/04-RESEARCH.md
    apps/agent/agent.py
    apps/agent/.env.example
  </read_first>
  <files>apps/agent/src/frappe_mcp.py, apps/agent/.env.example</files>
  <acceptance_criteria>
    - `apps/agent/src/frappe_mcp.py` exportiert `build_frappe_mcp_server`.
    - In `build_frappe_mcp_server` werden exakt `FRAPPE_MCP_URL`, `FRAPPE_API_KEY`, `FRAPPE_API_SECRET` gelesen.
    - Authorization-Header hat exakt das Format `token {api_key}:{api_secret}` (per D-03, D-04).
    - Bei fehlenden ENV-Werten wird `ValueError` mit fehlenden Key-Namen geworfen.
    - `apps/agent/.env.example` enthaelt die drei neuen FRAPPE-Keys als leere Defaults.
  </acceptance_criteria>
  <action>Erstelle `apps/agent/src/frappe_mcp.py` mit einer Funktion `build_frappe_mcp_server() -> mcp.MCPServerHTTP` (per D-03, D-04), die `FRAPPE_MCP_URL`, `FRAPPE_API_KEY`, `FRAPPE_API_SECRET` aus `os.getenv` liest, fehlende Werte als Liste sammelt und bei mindestens einem fehlenden Wert `ValueError("Missing required MCP env vars: ...")` wirft. Erzeuge danach `mcp.MCPServerHTTP(url=..., headers={"Authorization": f"token {api_key}:{api_secret}"})`. Aktualisiere `apps/agent/.env.example` mit exakt diesen Zeilen: `FRAPPE_MCP_URL=`, `FRAPPE_API_KEY=`, `FRAPPE_API_SECRET=`.</action>
  <verify>
    <automated>uv run pytest apps/agent/tests/test_mcp_integration.py::test_build_frappe_mcp_server_uses_env_headers -x -q</automated>
  </verify>
  <done>MCP-Factory erzeugt Serverobjekt aus festen ENV-Werten und bricht bei unvollstaendiger Konfiguration reproduzierbar ab.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP-Basistests fuer Factory und ENV-Vertrag anlegen</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-VALIDATION.md
    apps/agent/tests/test_agent.py
    apps/agent/src/frappe_mcp.py
    apps/agent/.env.example
  </read_first>
  <files>apps/agent/tests/test_mcp_integration.py</files>
  <acceptance_criteria>
    - `test_mcp_integration.py` enthaelt `test_build_frappe_mcp_server_uses_env_headers`.
    - `test_mcp_integration.py` enthaelt `test_build_frappe_mcp_server_missing_env_raises`.
    - Tests pruefen auf Header-Pattern `token key:secret` und `ValueError`.
    - `uv run pytest apps/agent/tests/test_mcp_integration.py -x -q` endet mit Exit-Code 0.
  </acceptance_criteria>
  <action>Erstelle `apps/agent/tests/test_mcp_integration.py` mit pytest-Tests fuer die neue Factory (per INTG-01/INTG-02): erstens erfolgreicher Build mit `FRAPPE_MCP_URL=https://example.test/mcp`, `FRAPPE_API_KEY=agent-key`, `FRAPPE_API_SECRET=agent-secret` und Assertion auf Authorization `token agent-key:agent-secret`; zweitens Fehlerfall mit fehlendem `FRAPPE_API_SECRET` und Assertion auf `ValueError` inklusive fehlendem Key-Namen. Nutze `patch.dict(os.environ, ..., clear=False)` fuer isolierte ENV-Szenarien.</action>
  <verify>
    <automated>uv run pytest apps/agent/tests/test_mcp_integration.py -x -q</automated>
  </verify>
  <done>Automatisierte Basistests sichern den MCP-Vertrag fuer URL/Auth und fehlende ENV-Werte ab.</done>
</task>

</tasks>

<verification>
Fuehre `uv run pytest apps/agent/tests/test_mcp_integration.py -x -q` aus und bestaetige, dass beide Basistests grün sind. Vor dem Commit zusaetzlich Anti-Drift-Reality-Check ausfuehren: `ls -la`, `ls -1 | wc -l`, `git ls-files | grep -E "^(HEAD|packed-refs|pre-commit\.sample|ORIG_HEAD|FETCH_HEAD|index)$"`; bei Treffern Commit abbrechen und Ursache beheben.
</verification>

<success_criteria>
- INTG-01: MCP-SDK-Nutzung ist ueber ein dediziertes Python-Modul produktiv vorbereitet.
- INTG-02: Authentifizierungspfad ueber feste ENV-Credentials ist technisch und testseitig verankert.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frappe-integration/04-01-SUMMARY.md`
</output>
