---
phase: 04-frappe-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/frappe_mcp.py
  - apps/agent/.env.example
  - apps/agent/tests/test_mcp_integration.py
autonomous: true
requirements:
  - INTG-01
  - INTG-02
must_haves:
  truths:
    - "Beim Start mit gueltiger Konfiguration kann der Agent MCP-Tools ohne Auth-Fehler nutzen."
    - "Wenn Credentials fehlen, meldet der Agent klar, welche MCP-Umgebungswerte fehlen."
    - "Der Agent nutzt fuer MCP-Aufrufe ausschliesslich die dedizierten Agent-Credentials aus ENV."
  artifacts:
    - path: "apps/agent/src/frappe_mcp.py"
      provides: "MCP-Factory fuer Stdio-Sidecar (FRAPPE_URL, FRAPPE_API_KEY, FRAPPE_API_SECRET)"
    - path: "apps/agent/.env.example"
      provides: "Verbindlicher ENV-Vertrag fuer MCP-Verbindung"
    - path: "apps/agent/tests/test_mcp_integration.py"
      provides: "Automatisierte Basisabdeckung fuer MCP-Konfiguration"
  key_links:
    - from: "apps/agent/.env.example"
      to: "apps/agent/src/frappe_mcp.py"
      via: "FRAPPE_URL, FRAPPE_API_KEY, FRAPPE_API_SECRET"
      pattern: "os\\.getenv\\(\"FRAPPE_(URL|API_KEY|API_SECRET)\"\\)"
---

<objective>
Lege die technische Grundlage fuer die MCP-Anbindung des Agenten fest, damit Session-Integration in den Folgeplaenen deterministisch umgesetzt werden kann.

Purpose: Verbindlicher ENV- und Factory-Vertrag reduziert Implementierungsrisiko fuer Auth und Connection-Lifecycle.
Output: MCP-Factory-Modul, ENV-Schluessel in `.env.example`, und MCP-Basistests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frappe-integration/04-CONTEXT.md
@.planning/phases/04-frappe-integration/04-RESEARCH.md
@apps/agent/agent.py
@apps/agent/.env.example

<interfaces>
From apps/agent/agent.py:
```python
def load_agent_instructions():
    ...

async def entrypoint(ctx: JobContext):
    ...
```

From locked runtime contract (D-05/D-06):
```python
mcp.MCPServerStdio(
    command="npx",
    args=["-y", "frappe-mcp-server"],
    env={
        "FRAPPE_URL": frappe_url,
        "FRAPPE_API_KEY": api_key,
        "FRAPPE_API_SECRET": api_secret,
    },
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP-Basistests fuer Stdio-Sidecar und ENV-Vertrag anlegen</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-VALIDATION.md
    .planning/phases/04-frappe-integration/04-CONTEXT.md
    apps/agent/tests/test_agent.py
  </read_first>
  <files>apps/agent/tests/test_mcp_integration.py</files>
  <acceptance_criteria>
    - `test_mcp_integration.py` enthaelt `test_build_frappe_mcp_server_uses_env_headers`.
    - `test_mcp_integration.py` enthaelt `test_build_frappe_mcp_server_missing_env_raises`.
    - Tests erwarten ausschliesslich den Produktivpfad `MCPServerStdio` mit `npx`/`frappe-mcp-server` und exakt `FRAPPE_URL`, `FRAPPE_API_KEY`, `FRAPPE_API_SECRET` in `env` (per D-05, D-06); kein `MCPServerHTTP` und keine Agent->MCP-HTTP-Auth im Python-Contract.
    - `uv run pytest apps/agent/tests/test_mcp_integration.py --collect-only -q` endet mit Exit-Code 0 (Tests sind syntaktisch valide und discoverable).
  </acceptance_criteria>
  <action>Erstelle oder erweitere `apps/agent/tests/test_mcp_integration.py` mit pytest-Tests fuer die geplante Factory (per INTG-01/INTG-02): (1) erfolgreicher Build mit `FRAPPE_URL=https://example.test`, `FRAPPE_API_KEY=agent-key`, `FRAPPE_API_SECRET=agent-secret` und Assertion, dass `MCPServerStdio` mit `command="npx"`, `args=["-y", "frappe-mcp-server"]` sowie `env` mit genau diesen drei Keys/Werten erzeugt wird; (2) Fehlerfall mit fehlendem `FRAPPE_API_SECRET` und Assertion auf `ValueError` inklusive fehlendem Key-Namen. Nutze `patch.dict(os.environ, ..., clear=False)`. Keine Assertions auf HTTP-Header zwischen Agent und MCP; Frappe-REST-Auth (`token key:secret`) liegt im Node-Sidecar, nicht im Python-MCP-Client.</action>
  <verify>
    <automated>uv run pytest apps/agent/tests/test_mcp_integration.py --collect-only -q</automated>
  </verify>
  <done>Basistests und Erwartungsvertrag fuer Stdio-Sidecar-ENV sind angelegt und von pytest aufloesbar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP-Factory mit festen ENV-Credentials implementieren</name>
  <read_first>
    .planning/phases/04-frappe-integration/04-CONTEXT.md
    .planning/phases/04-frappe-integration/04-RESEARCH.md
    apps/agent/agent.py
    apps/agent/.env.example
    apps/agent/tests/test_mcp_integration.py
  </read_first>
  <files>apps/agent/src/frappe_mcp.py, apps/agent/.env.example</files>
  <acceptance_criteria>
    - `apps/agent/src/frappe_mcp.py` exportiert `build_frappe_mcp_server`.
    - `build_frappe_mcp_server() -> mcp.MCPServerStdio` liest exakt `FRAPPE_URL`, `FRAPPE_API_KEY`, `FRAPPE_API_SECRET` aus der Umgebung (per D-03, D-04).
    - Produktivpfad ist ausschliesslich stdio-Sidecar: `MCPServerStdio(command="npx", args=["-y", "frappe-mcp-server"], env={...})` ohne Agent->MCP-HTTP (per D-05, D-06).
    - Bei fehlenden ENV-Werten wird `ValueError` mit fehlenden Key-Namen geworfen.
    - `apps/agent/.env.example` enthaelt die drei Keys als leere Defaults: `FRAPPE_URL=`, `FRAPPE_API_KEY=`, `FRAPPE_API_SECRET=`.
  </acceptance_criteria>
  <action>Erstelle `apps/agent/src/frappe_mcp.py` mit `build_frappe_mcp_server() -> mcp.MCPServerStdio` (per D-03, D-04, D-05, D-06), die die drei FRAPPE-*-Werte aus `os.getenv` liest, fehlende Werte sammelt und bei mindestens einem fehlenden Wert `ValueError("Missing required MCP env vars: ...")` wirft. Gib `mcp.MCPServerStdio(command="npx", args=["-y", "frappe-mcp-server"], env={"FRAPPE_URL": frappe_url, "FRAPPE_API_KEY": api_key, "FRAPPE_API_SECRET": api_secret})` zurueck. Aktualisiere `apps/agent/.env.example` mit den drei Zeilen. Implementiere nichts fuer HTTP-MCP vom Agent aus.</action>
  <verify>
    <automated>uv run pytest apps/agent/tests/test_mcp_integration.py -x -q</automated>
  </verify>
  <done>MCP-Factory erzeugt das Sidecar-`MCPServerStdio` aus festen ENV-Werten und bricht bei unvollstaendiger Konfiguration reproduzierbar ab; alle Basistests sind gruen.</done>
</task>

</tasks>

<verification>
Fuehre `uv run pytest apps/agent/tests/test_mcp_integration.py -x -q` aus und bestaetige, dass beide Basistests grün sind. Vor dem Commit zusaetzlich Anti-Drift-Reality-Check ausfuehren: `ls -la`, `ls -1 | wc -l`, `git ls-files | grep -E "^(HEAD|packed-refs|pre-commit\.sample|ORIG_HEAD|FETCH_HEAD|index)$"`; bei Treffern Commit abbrechen und Ursache beheben.
</verification>

<success_criteria>
- INTG-01: MCP-SDK-Nutzung ist ueber ein dediziertes Python-Modul produktiv vorbereitet.
- INTG-02: Authentifizierungspfad ueber feste ENV-Credentials ist technisch und testseitig verankert.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frappe-integration/04-01-SUMMARY.md`
</output>
