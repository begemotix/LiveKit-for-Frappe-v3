---
phase: 04-frappe-integration
plan: 08
type: execute
wave: 9
depends_on:
  - 04-09
files_modified:
  - .planning/phases/04-frappe-integration/04-HUMAN-UAT.md
  - .planning/phases/04-frappe-integration/04-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - INTG-05
must_haves:
  truths:
    - "Wave C weist 403-Produktverhalten nach: klare Nutzerbotschaft, kein Retry, strukturierter Logeintrag, stabile Session."
    - "Fehlerpfade sind als produktives Verhalten dokumentiert, nicht als technische Ausnahme."
  artifacts:
    - path: ".planning/phases/04-frappe-integration/04-HUMAN-UAT.md"
      provides: "Wave-C-Nachweise fuer 403 no-retry + Voice/Text + Session-Stabilitaet"
    - path: ".planning/phases/04-frappe-integration/04-VERIFICATION.md"
      provides: "INTG-05 Abschlussstatus mit referenzierter 403-Evidenz"
  key_links:
    - from: ".planning/phases/04-frappe-integration/04-HUMAN-UAT.md"
      to: ".planning/phases/04-frappe-integration/04-VERIFICATION.md"
      via: "gleicher 403-Fall inklusive Pflichtfelder event/correlation_id/tool/error_class"
      pattern: "403|correlation_id|error_class"
---

<objective>
Schliesse die Gap-Closure fuer Wave C durch belastbare 403-Produktnachweise in UAT und Verification.

Purpose: Erfuellt die verbleibende Live-Luecke fuer INTG-05 ohne Scope-Erweiterung.
Output: Dokumentierte 403-Evidenzkette mit no-retry und stabiler Session.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-frappe-integration/04-CONTEXT.md
@.planning/phases/04-frappe-integration/04-VERIFICATION.md
@.planning/phases/04-frappe-integration/04-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 403-Produktverhalten als Wave-C-Evidenz in UAT abschliessen</name>
  <files>.planning/phases/04-frappe-integration/04-HUMAN-UAT.md</files>
  <read_first>.planning/phases/04-frappe-integration/04-HUMAN-UAT.md, .planning/phases/04-frappe-integration/04-VERIFICATION.md, .planning/phases/04-frappe-integration/04-CONTEXT.md</read_first>
  <action>Den 403-Testfall in `04-HUMAN-UAT.md` als abgeschlossenen `Wave C`-Nachweis erfassen mit exakt dieser Nutzerbotschaft fuer Voice und Text: `Darauf habe ich mit meinem Agent-Zugang leider keinen Zugriff.` (D-10). Zusaetzlich den no-retry-Nachweis als explizite Aussage aufnehmen (`kein Retry auf 403`, D-11) und einen Logauszug mit den Pflichtfeldern `event`, `correlation_id`, `tool`, `error_class` eintragen. Abschliessend Session-Stabilitaet als `result: pass` dokumentieren.</action>
  <verify>
    <automated>rg -n "Wave C|403|kein Retry|event|correlation_id|tool|error_class|Session" .planning/phases/04-frappe-integration/04-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Darauf habe ich mit meinem Agent-Zugang leider keinen Zugriff\\." .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` liefert Treffer.
    - `rg -n "kein Retry auf 403|event|correlation_id|tool|error_class" .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` liefert Treffer.
    - `rg -n "Wave C|result: pass|Session" .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` liefert Treffer.
  </acceptance_criteria>
  <done>Die 403-Evidenz ist vollstaendig, reproduzierbar und als Produktverhalten formuliert.</done>
</task>

<task type="auto">
  <name>Task 2: Verification auf INTG-05 Wave-C-Abschluss aktualisieren</name>
  <files>.planning/phases/04-frappe-integration/04-VERIFICATION.md</files>
  <read_first>.planning/phases/04-frappe-integration/04-VERIFICATION.md, .planning/phases/04-frappe-integration/04-HUMAN-UAT.md, .planning/REQUIREMENTS.md</read_first>
  <action>In `04-VERIFICATION.md` die Statuszeilen fuer D-10 (feste Nutzerbotschaft), D-11 (kein Retry) und D-12 (strukturierte Logs mit Pflichtfeldern) aktualisieren und `INTG-05` auf Basis des referenzierten Wave-C-UAT-Belegs abschliessen. Referenz auf den konkreten 403-Fall mit Logbeleg aufnehmen; Architekturpfad explizit auf `stdio-sidecar` fixieren (`MCPServerStdio` mit `FRAPPE_URL`, `FRAPPE_API_KEY`, `FRAPPE_API_SECRET`) und klar festhalten: kein HTTP-Endpoint Agent->MCP, keine lokale Bridge, kein REST-Fallback (per D-05, D-06).</action>
  <verify>
    <automated>rg -n "D-08|D-09|D-10|INTG-05|Wave C|403" .planning/phases/04-frappe-integration/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "D-10|D-11|D-12|INTG-05|403|Wave C" .planning/phases/04-frappe-integration/04-VERIFICATION.md` liefert Treffer.
    - `rg -n "Darauf habe ich mit meinem Agent-Zugang leider keinen Zugriff\\.|kein Retry|event|correlation_id|tool|error_class" .planning/phases/04-frappe-integration/04-VERIFICATION.md` liefert Treffer.
    - `rg -n "stdio-sidecar|kein HTTP-Endpoint Agent->MCP|keine lokale Bridge|kein REST-Fallback" .planning/phases/04-frappe-integration/04-VERIFICATION.md` bestaetigt unveraenderten Scope.
  </acceptance_criteria>
  <done>INTG-05 ist in Verification nur noch durch referenzierte Live-Evidenz begruendet.</done>
</task>

</tasks>

<verification>
Vergleiche UAT- und Verification-Abschnitte auf identische 403-Evidenz und konsistente Begrifflichkeit.
Vor jedem Commit zwingend Anti-Drift-Reality-Check ausfuehren: `ls -la`, `ls -1 | wc -l`, `git ls-files | grep -E "^(HEAD|packed-refs|pre-commit\.sample|ORIG_HEAD|FETCH_HEAD|index)$"`; bei Treffern Commit abbrechen.
</verification>

<success_criteria>
- INTG-05 ist mit durchgaengiger, nachvollziehbarer 403-Evidenz geschlossen.
- Wave C ist als Abschluss der Code-Hardening-Wellen dokumentiert.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frappe-integration/04-08-SUMMARY.md`
</output>
