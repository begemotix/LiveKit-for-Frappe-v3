---
phase: 04-frappe-integration
plan: 06
type: execute
wave: 6
depends_on:
  - 04-05
files_modified:
  - .planning/phases/04-frappe-integration/04-HUMAN-UAT.md
  - .planning/phases/04-frappe-integration/04-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - INTG-01
  - INTG-02
must_haves:
  truths:
    - "Der Agent baut in einer neuen Session die MCP-Verbindung ueber stdio-Sidecar auf, ohne dass Nutzer einen HTTP-Endpunkt konfigurieren muessen."
    - "Bei jeder Anfrage nutzt der Agent ausschliesslich `FRAPPE_URL`, `FRAPPE_API_KEY` und `FRAPPE_API_SECRET`; ein Runtime-Switch ist fuer Nutzer nicht verfuegbar."
  artifacts:
    - path: ".planning/phases/04-frappe-integration/04-HUMAN-UAT.md"
      provides: "Wave-A-Testnachweis fuer ENV-Contract und Connectivity"
    - path: ".planning/phases/04-frappe-integration/04-VERIFICATION.md"
      provides: "Aktualisierter Status fuer INTG-01/INTG-02 nach Wave A"
  key_links:
    - from: ".planning/phases/04-frappe-integration/04-HUMAN-UAT.md"
      to: ".planning/phases/04-frappe-integration/04-VERIFICATION.md"
      via: "gemeinsame Connectivity- und Credential-Evidenz"
      pattern: "FRAPPE_URL|FRAPPE_API_KEY|FRAPPE_API_SECRET"
---

<objective>
Fuehre nach Wave-D-Freigabe die Gap-Closure fuer Wave A durch: verbindlicher ENV-Vertrag und Connectivity-Nachweis fuer den gewaehlten stdio-Sidecar-Transport.

Purpose: Schafft die belastbare Startbasis fuer Session-Lifecycle- und Permission-Waves.
Output: Abnahmefaehige Nachweise fuer INTG-01/INTG-02 ohne Architekturdrift.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-frappe-integration/04-CONTEXT.md
@.planning/phases/04-frappe-integration/04-RESEARCH.md
@.planning/phases/04-frappe-integration/04-VERIFICATION.md
@.planning/phases/04-frappe-integration/04-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-A Connectivity- und ENV-Vertrag nachweisen</name>
  <files>.planning/phases/04-frappe-integration/04-HUMAN-UAT.md</files>
  <read_first>.planning/phases/04-frappe-integration/04-HUMAN-UAT.md, .planning/phases/04-frappe-integration/04-VERIFICATION.md, .planning/phases/04-frappe-integration/04-CONTEXT.md</read_first>
  <action>Im UAT-Dokument einen expliziten Abschnitt `Wave A` eintragen, der die ENV-Quellen exakt als `FRAPPE_URL`, `FRAPPE_API_KEY`, `FRAPPE_API_SECRET` benennt (D-03/D-04) und festhaelt, dass kein Runtime-Switch auf Frontend/User-Credentials stattfindet. Den produktiven Transport als `stdio-sidecar` mit `MCPServerStdio(command="npx", args=["-y","frappe-mcp-server"], env={FRAPPE_URL, FRAPPE_API_KEY, FRAPPE_API_SECRET})` dokumentieren und einen konkreten Connectivity-Nachweis mit `captured_at`, `environment`, `connectivity_log_ref`, `result: pass` erfassen (per D-05, D-06).</action>
  <verify>
    <automated>rg -n "Wave A|FRAPPE_URL|FRAPPE_API_KEY|FRAPPE_API_SECRET|selected_transport: stdio-sidecar|connectivity" .planning/phases/04-frappe-integration/04-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Wave A|result: pass" .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` liefert Treffer.
    - `rg -n "FRAPPE_URL|FRAPPE_API_KEY|FRAPPE_API_SECRET" .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` liefert alle drei ENV-Keys.
    - `rg -n "selected_transport: stdio-sidecar|MCPServerStdio" .planning/phases/04-frappe-integration/04-HUMAN-UAT.md` liefert Treffer.
  </acceptance_criteria>
  <done>Wave A ist als reproduzierbarer Connectivity- und ENV-Vertragstest dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 2: Verification-Status fuer INTG-01/INTG-02 aktualisieren</name>
  <files>.planning/phases/04-frappe-integration/04-VERIFICATION.md</files>
  <read_first>.planning/phases/04-frappe-integration/04-VERIFICATION.md, .planning/phases/04-frappe-integration/04-HUMAN-UAT.md, .planning/REQUIREMENTS.md</read_first>
  <action>In `04-VERIFICATION.md` die Eintraege zu `INTG-01` und `INTG-02` auf den Wave-A-Nachweis verlinken und nur bei konsistentem UAT-Beleg auf `PASS`/abgeschlossen setzen. Die Architektur-Guardrails explizit wiederholen: `stdio-Sidecar als Produktivpfad`, `kein HTTP-Endpoint Agent->MCP`, `keine lokale Bridge`, `kein REST-Fallback`, `keine lokale Tool-Allowlist` (D-05 bis D-07). Falls ein Beleg fehlt, Status nicht auf abgeschlossen setzen.</action>
  <verify>
    <automated>rg -n "INTG-01|INTG-02|Wave A|stdio|HTTP-Endpoint|Bridge|REST|Allowlist" .planning/phases/04-frappe-integration/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "INTG-01|INTG-02" .planning/phases/04-frappe-integration/04-VERIFICATION.md` zeigt beide Requirement-IDs.
    - `rg -n "Wave A|04-HUMAN-UAT.md" .planning/phases/04-frappe-integration/04-VERIFICATION.md` zeigt Referenz auf UAT-Evidenz.
    - `rg -n "stdio-Sidecar als Produktivpfad|kein HTTP-Endpoint Agent->MCP|keine lokale Bridge|kein REST-Fallback|keine lokale Tool-Allowlist" .planning/phases/04-frappe-integration/04-VERIFICATION.md` liefert Treffer.
  </acceptance_criteria>
  <done>Verification und UAT sind fuer Wave A synchron und widerspruchsfrei.</done>
</task>

</tasks>

<verification>
Pruefe, dass alle Wave-A-Nachweise auf Wave-D-Entscheidungen referenzieren und keine neue Architekturvariante dokumentiert wurde.
Vor jedem Commit zwingend Anti-Drift-Reality-Check ausfuehren: `ls -la`, `ls -1 | wc -l`, `git ls-files | grep -E "^(HEAD|packed-refs|pre-commit\.sample|ORIG_HEAD|FETCH_HEAD|index)$"`; bei Treffern Commit abbrechen.
</verification>

<success_criteria>
- INTG-01/INTG-02 sind mit Live-Evidenz abgesichert.
- Die Reihenfolge D -> A bleibt im Artefakt explizit nachvollziehbar.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frappe-integration/04-06-SUMMARY.md`
</output>
