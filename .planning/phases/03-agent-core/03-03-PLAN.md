---
phase: 03-agent-core
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/agent/tests/test_agent.py
  - apps/agent/WHITE_LABELING.md
autonomous: true
requirements: [AGNT-01, AGNT-03, AGNT-04]
must_haves:
  truths:
    - "Automated tests verify core agent behaviors"
    - "White-labeling options are documented for partners"
  artifacts:
    - path: "apps/agent/tests/test_agent.py"
      provides: "Automated verification suite"
    - path: "apps/agent/WHITE_LABELING.md"
      provides: "Partner documentation"
---

<objective>
Verify the agent implementation through automated tests and provide documentation for white-labeling configuration.

Purpose: Ensuring reliability and facilitating deployment for partners.
Output: Automated test suite and white-labeling guide.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-core/03-02-SUMMARY.md
@.planning/phases/03-agent-core/03-CONTEXT.md
@.planning/phases/03-agent-core/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Automated Verification Suite</name>
  <files>apps/agent/tests/test_agent.py</files>
  <read_first>
    - apps/agent/agent.py
    - apps/agent/tests/test_agent.py
    - .planning/phases/03-agent-core/03-VALIDATION.md
  </read_first>
  <action>
    Update `apps/agent/tests/test_agent.py` (stubs created in Wave 0).
    1. Implement `test_join` to verify `WorkerOptions` has `entrypoint` set to the correct function.
    2. Implement `test_mock_tool` to verify `mock_data_lookup` is reachable and returns the expected JSON structure.
    3. Implement `test_persona_injection` by creating a mock environment and verifying the variable substitution logic used in `agent.py`.
    4. Implement `test_interruption` (AGNT-03) to verify that a mock interruption signal stops the output stream (using mocks for the LiveKit session).
  </action>
  <verify>
    <automated>pytest apps/agent/tests/test_agent.py</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/agent/tests/test_agent.py` is fully implemented.
    - All 4 tests (`test_join`, `test_mock_tool`, `test_persona_injection`, `test_interruption`) pass.
  </acceptance_criteria>
  <done>Automated verification suite is operational.</done>
</task>

<task type="auto">
  <name>Task 2: Create White-Labeling Documentation</name>
  <files>apps/agent/WHITE_LABELING.md</files>
  <read_first>
    - apps/agent/.env.example
    - apps/agent/WHITE_LABELING.md
    - .planning/phases/03-agent-core/03-CONTEXT.md
  </read_first>
  <action>
    Create `apps/agent/WHITE_LABELING.md`.
    1. List all configuration variables: `AGENT_NAME`, `COMPANY_NAME`, `ROLE_DESCRIPTION`, `INITIAL_GREETING`, `MANDATORY_ANNOUNCEMENT`, `AGENT_VOICE`, `VAD_THRESHOLD`, `VAD_SILENCE_DURATION_MS`.
    2. Explain what each variable does and give examples.
    3. Include a section on "Legal Compliance" explaining the mandatory announcement requirement.
  </action>
  <verify>
    <automated>test -f apps/agent/WHITE_LABELING.md && grep "MANDATORY_ANNOUNCEMENT" apps/agent/WHITE_LABELING.md</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/agent/WHITE_LABELING.md` exists.
    - Contains descriptions for all variables in `.env.example`.
    - Includes examples for `ROLE_DESCRIPTION`.
  </acceptance_criteria>
  <done>Documentation for white-labeling is complete.</done>
</task>

</tasks>

<verification>
Run the full test suite and review the documentation for clarity.
</verification>

<success_criteria>
- All `pytest` tests pass.
- `WHITE_LABELING.md` provides clear instructions for partners.
- Phase 03 requirements (AGNT-01 to AGNT-04) are fully verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-core/03-03-SUMMARY.md`
</output>
