---
phase: 03-agent-core
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/agent/agent.py
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04]
must_haves:
  truths:
    - "Agent joins room upon participant entry"
    - "Agent plays non-interruptible mandatory announcement"
    - "Agent responds to speech via OpenAI Realtime"
    - "Agent stops audio immediately upon user interruption"
    - "Agent uses filler sentences before calling mock tool"
  artifacts:
    - path: "apps/agent/agent.py"
      provides: "Full voice agent logic"
      contains: "RealtimeModel"
  key_links:
    - from: "apps/agent/agent.py"
      to: "OpenAI Realtime API"
      via: "livekit-plugins-openai"
    - from: "apps/agent/agent.py"
      to: "LiveKit Server"
      via: "livekit-agents worker"
---

<objective>
Implement the core logic of the voice agent, including OpenAI Realtime integration, VAD, interruption handling, and the filler-audio trick.

Purpose: Creating a fully functional, low-latency voice agent.
Output: Complete `agent.py` implementation with all Phase 03 features.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<interfaces>
From apps/agent/agent.py (Skeleton created in Plan 01):
- `entrypoint(ctx: JobContext)`: The main entry point for the LiveKit agent worker.
</interfaces>

<context>
@.planning/phases/03-agent-core/03-01-SUMMARY.md
@.planning/phases/03-agent-core/03-CONTEXT.md
@.planning/phases/03-agent-core/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Realtime Model and Server VAD Configuration</name>
  <files>apps/agent/agent.py</files>
  <read_first>
    - apps/agent/agent.py
    - .planning/phases/03-agent-core/03-RESEARCH.md
  </read_first>
  <action>
    Update `apps/agent/agent.py` to use server-side VAD (avoiding a separate Silero layer):
    1. Initialize `openai.realtime.RealtimeModel`.
    2. Set `instructions` by reading `ROLE_DESCRIPTION` from ENV and replacing `{AGENT_NAME}` with `os.getenv("AGENT_NAME")` and `{COMPANY_NAME}` with `os.getenv("COMPANY_NAME")`.
    3. Configure `turn_detection` within `RealtimeModel` to use `openai.realtime.ServerVADOptions(
        threshold=float(os.getenv("VAD_THRESHOLD", 0.5)),
        silence_duration_ms=int(os.getenv("VAD_SILENCE_DURATION_MS", 500))
    )`.
    4. Set up `AgentSession` with `allow_interruptions=True`. Do NOT provide a separate `vad` plugin to the session to avoid double VAD processing.
  </action>
  <verify>
    <automated>grep "RealtimeModel" apps/agent/agent.py && grep "ServerVADOptions" apps/agent/agent.py</automated>
  </verify>
  <acceptance_criteria>
    - `RealtimeModel` is used with `ServerVADOptions`.
    - `VAD_THRESHOLD` and `VAD_SILENCE_DURATION_MS` are passed to `ServerVADOptions`.
    - No separate `vad` plugin is passed to `AgentSession` (avoiding double VAD).
    - `allow_interruptions=True` is set.
  </acceptance_criteria>
  <done>Agent session is correctly configured with server-side VAD.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Participant Join and DSGVO Announcement</name>
  <files>apps/agent/agent.py</files>
  <read_first>
    - apps/agent/agent.py
    - .planning/phases/03-agent-core/03-CONTEXT.md
  </read_first>
  <action>
    Enhance `apps/agent/agent.py` to:
    1. Listen for `ParticipantJoined` events from `ctx.room`.
    2. When a participant joins, start the session.
    3. Play `MANDATORY_ANNOUNCEMENT` from ENV using `session.say(announcement, allow_interruptions=False)`.
    4. Play `INITIAL_GREETING` from ENV (substituting variables as in Task 1) immediately after.
  </action>
  <verify>
    <automated>grep "ParticipantJoined" apps/agent/agent.py && grep "allow_interruptions=False" apps/agent/agent.py</automated>
  </verify>
  <acceptance_criteria>
    - `ParticipantJoined` event handler is implemented.
    - `MANDATORY_ANNOUNCEMENT` is played with `allow_interruptions=False`.
    - `INITIAL_GREETING` follows the announcement.
  </acceptance_criteria>
  <done>Agent lifecycle and mandatory compliance greeting implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Language-Agnostic Filler and Mock Tool</name>
  <files>apps/agent/agent.py</files>
  <read_first>
    - apps/agent/agent.py
    - .planning/phases/03-agent-core/03-CONTEXT.md
  </read_first>
  <action>
    Finalize `apps/agent/agent.py`:
    1. Append to system instructions: "IMPORTANT: When using a tool, always first say a brief natural filler in the user's language (e.g., 'Einen Moment, ich schaue nach' if German, 'One moment, I'll check that' if English) so the user knows you are working."
    2. Define an async function `mock_data_lookup(query: str)` that does `await asyncio.sleep(3)` and returns `{"status": "success", "data": "Found info for " + query}`.
    3. Register this function as a tool in the `RealtimeModel`.
    4. Add an `on_function_call_start` event handler as a placeholder.
  </action>
  <verify>
    <automated>grep "mock_data_lookup" apps/agent/agent.py && grep "on_function_call_start" apps/agent/agent.py</automated>
  </verify>
  <acceptance_criteria>
    - `mock_data_lookup` is defined and uses `asyncio.sleep(3)`.
    - Filler instructions are language-agnostic.
    - `on_function_call_start` hook is present and verified.
  </acceptance_criteria>
  <done>Filler logic and architectural tool preparation complete.</done>
</task>

</tasks>

<verification>
Manual verification of the agent in a LiveKit room to ensure natural conversation, correct interruption handling, and playing of the mandatory announcement.
</verification>

<success_criteria>
- Agent speaks the mandatory announcement on join.
- Agent is interruptible during normal conversation.
- Agent uses the filler-audio trick when "checking" the mock tool.
- Mock tool execution takes ~3 seconds as planned.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-core/03-02-SUMMARY.md`
</output>
