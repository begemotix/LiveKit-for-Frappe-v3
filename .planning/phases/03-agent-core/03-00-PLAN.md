---
phase: 03-agent-core
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/agent/tests/test_agent.py
autonomous: true
requirements: [AGNT-01]
must_haves:
  truths:
    - "Test framework is installed and discoverable"
  artifacts:
    - path: "apps/agent/tests/test_agent.py"
      provides: "Test stubs for AGNT-01, AGNT-03, AGNT-04"
---

<objective>
Initialize the testing infrastructure for the Agent Core phase, ensuring that subsequent waves can use automated pytest commands for verification.

Purpose: Establishing a test-first foundation as per Nyquist compliance.
Output: Pytest installed and test file stubs created.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-core/03-CONTEXT.md
@.planning/phases/03-agent-core/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Test Dependencies</name>
  <files></files>
  <action>
    Install `pytest` and `pytest-asyncio` in the environment. 
    Note: Since we are in a monorepo, ensure they are available for the `apps/agent` context.
  </action>
  <verify>
    <automated>pytest --version</automated>
  </verify>
  <done>Testing framework is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create Test Stubs</name>
  <files>apps/agent/tests/test_agent.py</files>
  <action>
    Create the `apps/agent/tests/` directory and an initial `test_agent.py` file with the following empty/failing test stubs marked with `@pytest.mark.asyncio`:
    - `test_join`
    - `test_interruption`
    - `test_mock_tool`
    - `test_persona_injection`
    
    This allows Wave 1/2 to run `pytest -k` without failing due to a missing file.
  </action>
  <verify>
    <automated>test -f apps/agent/tests/test_agent.py</automated>
  </verify>
  <done>Test stubs are available for subsequent waves.</done>
</task>

</tasks>

<success_criteria>
- `pytest` is installed.
- `apps/agent/tests/test_agent.py` exists with required stubs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-core/03-00-SUMMARY.md`
</output>
