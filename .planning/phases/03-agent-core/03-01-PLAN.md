---
phase: 03-agent-core
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - apps/agent/requirements.txt
  - apps/agent/.env.example
  - apps/agent/agent.py
autonomous: true
requirements: [AGNT-01]
must_haves:
  truths:
    - "Directory apps/agent exists and is ready for development"
  artifacts:
    - path: "apps/agent/requirements.txt"
      provides: "Dependency management"
    - path: "apps/agent/.env.example"
      provides: "Configuration template for white-labeling"
    - path: "apps/agent/agent.py"
      provides: "Agent worker skeleton"
---

<objective>
Setup the foundation for the Python Agent Worker, including directory structure, dependencies, and configuration interface via environment variables.

Purpose: Establishing the technical base and white-labeling interface for the agent.
Output: Working directory structure, dependency list, and configuration template.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-core/03-CONTEXT.md
@.planning/phases/03-agent-core/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Agent Directory from Template</name>
  <files>apps/agent/requirements.txt</files>
  <read_first>
    - .cursor/rules
    - .planning/phases/03-agent-core/03-CONTEXT.md
  </read_first>
  <action>
    1. Clone the official LiveKit template: `git clone https://github.com/livekit-examples/agent-starter-python apps/agent`.
    2. Remove the `.git` directory to decouple from the upstream repository: `rm -rf apps/agent/.git`.
    3. Verify that `requirements.txt` and `agent.py` exist in `apps/agent`.
    4. Ensure dependencies from RESEARCH.md are present or added to `requirements.txt`:
       - `livekit-agents[openai]~=1.4.0` (Note: silero removed to avoid double VAD)
       - `python-json-logger~=2.0.0`
       - `python-dotenv~=1.0.0`
  </action>
  <verify>
    <automated>test -d apps/agent && test -f apps/agent/requirements.txt && test -f apps/agent/agent.py</automated>
  </verify>
  <acceptance_criteria>
    - Directory `apps/agent` exists.
    - File `apps/agent/requirements.txt` exists.
    - File `apps/agent/agent.py` exists.
    - `.git` directory is removed from `apps/agent`.
  </acceptance_criteria>
  <done>Agent directory initialized from template.</done>
</task>

<task type="auto">
  <name>Task 2: Define Configuration Interface (.env.example)</name>
  <files>apps/agent/.env.example</files>
  <read_first>
    - .planning/phases/03-agent-core/03-CONTEXT.md
    - apps/agent/.env.example
  </read_first>
  <action>
    Create or update `apps/agent/.env.example` with these exact variables and defaults:
    - `LIVEKIT_URL=`
    - `LIVEKIT_API_KEY=`
    - `LIVEKIT_API_SECRET=`
    - `OPENAI_API_KEY=`
    - `AGENT_NAME=Assistant`
    - `COMPANY_NAME=Frappe`
    - `ROLE_DESCRIPTION=You are {AGENT_NAME}, a helpful assistant for {COMPANY_NAME}.`
    - `INITIAL_GREETING=Hello, I am {AGENT_NAME}. How can I help you today?`
    - `MANDATORY_ANNOUNCEMENT=Hinweis: Sie sprechen mit einem KI-Assistenten. Audio-Daten werden zur Verarbeitung an OpenAI in den USA übertragen. Durch Fortsetzen des Gesprächs willigen Sie ein.`
    - `AGENT_VOICE=alloy`
    - `VAD_THRESHOLD=0.5`
    - `VAD_SILENCE_DURATION_MS=500`
  </action>
  <verify>
    <automated>grep "MANDATORY_ANNOUNCEMENT=Hinweis: Sie sprechen mit einem KI-Assistenten" apps/agent/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/agent/.env.example` exists.
    - `MANDATORY_ANNOUNCEMENT` has the correct German default.
    - Contains `AGENT_NAME=Assistant`.
    - Contains `VAD_SILENCE_DURATION_MS=500`.
  </acceptance_criteria>
  <done>Configuration interface is defined and documented with correct GDPR defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Create Agent Skeleton and Logging</name>
  <files>apps/agent/agent.py</files>
  <read_first>
    - .planning/phases/03-agent-core/03-CONTEXT.md
    - .planning/phases/03-agent-core/03-RESEARCH.md
    - apps/agent/agent.py
  </read_first>
  <action>
    Update `apps/agent/agent.py` (from template):
    1. Import `logging`, `os`, `dotenv`, `pythonjsonlogger`.
    2. Configure logging with `jsonlogger.JsonFormatter(fmt='%(asctime)s %(levelname)s %(name)s %(message)s')`.
    3. Load `.env` using `load_dotenv()`.
    4. Implement/Update the LiveKit `WorkerOptions` and `entrypoint`.
    5. Within `entrypoint`, derive a correlation ID from `ctx.room.name`.
  </action>
  <verify>
    <automated>grep "JsonFormatter" apps/agent/agent.py && grep "load_dotenv" apps/agent/agent.py</automated>
  </verify>
  <acceptance_criteria>
    - `JsonFormatter` is initialized in `agent.py`.
    - `load_dotenv()` is called.
    - Room name is used for correlation logging.
  </acceptance_criteria>
  <done>Agent skeleton with logging is ready for core logic implementation.</done>
</task>

</tasks>

<verification>
Check that the `apps/agent` directory is correctly structured and contains the necessary files for the next wave.
</verification>

<success_criteria>
- Directory `apps/agent` exists.
- `requirements.txt` contains correct library versions.
- `.env.example` reflects all required white-labeling variables.
- `agent.py` exists with structured logging initialized.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-core/03-01-SUMMARY.md`
</output>
