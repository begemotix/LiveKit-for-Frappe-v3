---
phase: 05-eu-voice-agent-typ-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/pyproject.toml
  - apps/agent/.env.example
  - apps/agent/src/mode_config.py
  - apps/agent/src/model_factory.py
  - apps/agent/tests/test_mode_switch.py
  - apps/agent/tests/test_mode_env_validation.py
autonomous: true
requirements:
  - AGNT-05
must_haves:
  truths:
    - "Der Worker waehlt den Agent-Modus deterministisch ueber ENV und nutzt Typ B als Default."
    - "Typ B startet nur mit vollstaendiger Mistral/Voxtral-Konfiguration und faellt bei fehlenden Keys klar fehl."
    - "Voice-Konfiguration respektiert Ref-Audio-Prioritaet vor Voice-ID."
  artifacts:
    - path: "apps/agent/src/mode_config.py"
      provides: "Mode- und ENV-Vertrag inkl. Validierung pro Modus"
    - path: "apps/agent/src/model_factory.py"
      provides: "Factory fuer OpenAI-Realtime (Typ A) und Mistral/Voxtral (Typ B)"
    - path: "apps/agent/tests/test_mode_switch.py"
      provides: "Testabdeckung fuer Mode-Auswahl inkl. Typ-B-Default"
    - path: "apps/agent/tests/test_mode_env_validation.py"
      provides: "Testabdeckung fuer Pflicht-ENV und Voice-Prioritaet"
    - path: "apps/agent/.env.example"
      provides: "Dokumentierter Runtime-Vertrag fuer beide Modi"
  key_links:
    - from: "apps/agent/src/mode_config.py"
      to: "apps/agent/src/model_factory.py"
      via: "resolve_agent_mode() + validate_mode_env()"
      pattern: "AGENT_MODE|MISTRAL_API_KEY|VOXTRAL_STT_MODEL|VOXTRAL_TTS_MODEL"
    - from: "apps/agent/.env.example"
      to: "apps/agent/src/mode_config.py"
      via: "identische ENV-Schluessel und Defaults"
      pattern: "AGENT_MODE=type_b"
---

<objective>
Erzeuge die technische Grundlage fuer den EU-Agent-Modus (Typ B) mit klar getrenntem ENV-Vertrag, mode-basierter Pipeline-Factory und belastbarer Testabdeckung.

Purpose: AGNT-05 wird ohne Architekturdrift vorbereitet, indem Moduswahl, Validierung und Model-Setup vor der Entrypoint-Integration sauber isoliert werden.
Output: Neue Config/Factory-Module, aktualisierte Abhaengigkeiten, ENV-Vertrag sowie Wave-0-Tests fuer Mode-Switch und Validierung.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-eu-voice-agent-typ-b/05-CONTEXT.md
@.planning/phases/05-eu-voice-agent-typ-b/05-RESEARCH.md
@.planning/phases/05-eu-voice-agent-typ-b/05-VALIDATION.md
@apps/agent/agent.py
@apps/agent/src/frappe_mcp.py
@apps/agent/pyproject.toml
@apps/agent/.env.example

<interfaces>
From apps/agent/agent.py:
```python
async def entrypoint(ctx: JobContext):
    model = openai.realtime.RealtimeModel(...)
    frappe_server = build_frappe_mcp_server()
    frappe_toolset = mcp.MCPToolset(id="frappe_mcp", mcp_server=frappe_server)
```

Required new contracts for this plan:
```python
def resolve_agent_mode() -> str  # returns "type_a" or "type_b", default "type_b" per D-F
def validate_mode_env(mode: str) -> None
def resolve_voice_config() -> dict[str, str | None]  # D-08 ref_audio overrides voice_id
def build_voice_pipeline(mode: str) -> dict[str, object]  # llm/stt/tts tuple
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0-Tests fuer Mode-Vertrag und Voice-Prioritaet anlegen</name>
  <read_first>
    .planning/phases/05-eu-voice-agent-typ-b/05-CONTEXT.md
    .planning/phases/05-eu-voice-agent-typ-b/05-VALIDATION.md
    apps/agent/tests/test_agent.py
    apps/agent/src/frappe_mcp.py
  </read_first>
  <files>apps/agent/tests/test_mode_switch.py, apps/agent/tests/test_mode_env_validation.py</files>
  <acceptance_criteria>
    - `apps/agent/tests/test_mode_switch.py` enthaelt `test_default_mode_is_type_b` (D-F) und `test_agent_mode_type_a_selects_openai`.
    - `apps/agent/tests/test_mode_env_validation.py` enthaelt `test_type_b_requires_mistral_and_voxtral_env` (D-06), `test_ref_audio_overrides_voice_id` (D-08) und `test_type_b_accepts_de_and_en_language_smoke` (D-10).
    - Kein Test erwartet stillen Fallback von Typ B auf Typ A im Fehlerfall (D-04).
    - `cd apps/agent && uv run pytest tests/test_mode_switch.py tests/test_mode_env_validation.py --collect-only -q` endet mit Exit-Code 0.
  </acceptance_criteria>
  <action>Erstelle die zwei Testdateien fuer AGNT-05 mit konkreten ENV-Szenarien: `AGENT_MODE=type_b` als Defaultpfad (D-F), expliziter Typ-A-Pfad, Pflicht-Keys fuer Typ B (`MISTRAL_API_KEY`, `VOXTRAL_STT_MODEL`, `VOXTRAL_TTS_MODEL`) per D-06, sowie Voice-Prioritaet `AGENT_VOICE_REF_AUDIO` vor `AGENT_VOICE_ID` per D-08. Fuege zudem einen Smoke-Contract-Test hinzu, der fuer Typ B Sprachkonfiguration mit `AGENT_LANGUAGE=de` und `AGENT_LANGUAGE=en` akzeptiert (D-10). Schreibe die Tests so, dass sie gegen kommende Hilfsfunktionen in `src/mode_config.py`/`src/model_factory.py` laufen koennen.</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_mode_switch.py tests/test_mode_env_validation.py --collect-only -q</automated>
  </verify>
  <done>Wave-0-Testgeruest fuer Modusauswahl, Pflicht-ENV und Voice-Prioritaet ist vorhanden und pytest-discoverable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mode-Konfiguration und Pipeline-Factory fuer Typ A/B implementieren</name>
  <read_first>
    .planning/phases/05-eu-voice-agent-typ-b/05-CONTEXT.md
    .planning/phases/05-eu-voice-agent-typ-b/05-RESEARCH.md
    apps/agent/pyproject.toml
    apps/agent/.env.example
    apps/agent/agent.py
    apps/agent/tests/test_mode_switch.py
    apps/agent/tests/test_mode_env_validation.py
  </read_first>
  <files>apps/agent/pyproject.toml, apps/agent/.env.example, apps/agent/src/mode_config.py, apps/agent/src/model_factory.py</files>
  <acceptance_criteria>
    - `apps/agent/pyproject.toml` enthaelt `livekit-agents[mcp,openai,mistralai,silero,turn-detector]~=1.5` (D-06, Research Stack).
    - `apps/agent/src/mode_config.py` validiert mode-spezifisch: Typ A braucht `OPENAI_API_KEY`; Typ B braucht `MISTRAL_API_KEY`, `VOXTRAL_STT_MODEL`, `VOXTRAL_TTS_MODEL` (D-06).
    - `resolve_agent_mode()` nutzt `AGENT_MODE` und liefert bei fehlendem Wert `type_b` (D-F).
    - `resolve_voice_config()` setzt `ref_audio` vor `voice_id`, wenn beide gesetzt sind (D-08), ohne begemotix-spezifische Voice-Defaults (D-07).
    - `apps/agent/src/model_factory.py` erzeugt getrennte Provider-Pipelines fuer Typ A (OpenAI Realtime) und Typ B (Mistral LLM + Voxtral STT/TTS) und implementiert keinen stillen Provider-Fallback (D-04).
    - `apps/agent/.env.example` dokumentiert mindestens `AGENT_MODE=type_b`, `MISTRAL_API_KEY=`, `MISTRAL_LLM_MODEL=`, `VOXTRAL_STT_MODEL=`, `VOXTRAL_TTS_MODEL=`, `AGENT_LANGUAGE=`, `AGENT_VOICE_ID=`, `AGENT_VOICE_REF_AUDIO=`.
  </acceptance_criteria>
  <action>Implementiere `src/mode_config.py` mit klaren Hilfsfunktionen fuer Modusauswahl und ENV-Validierung (D-06, D-07, D-08, D-F): `resolve_agent_mode()`, `validate_mode_env(mode)`, `resolve_voice_config()`. Implementiere `src/model_factory.py` mit `build_voice_pipeline(mode)` und konkreter Provider-Zuordnung: Typ A => `openai.realtime.RealtimeModel`; Typ B => Mistral-Pluginobjekte fuer LLM/STT/TTS mit `MISTRAL_LLM_MODEL`, `VOXTRAL_STT_MODEL`, `VOXTRAL_TTS_MODEL`, optionaler Sprach-/Voice-Konfiguration aus `resolve_voice_config()`. Aktualisiere Dependencies in `pyproject.toml` und den ENV-Vertrag in `.env.example`. Kein Routing-Resilienz-Layer und kein OpenRouter-Fallback (D-05), kein Session-Switch-Feature (deferred).</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_mode_switch.py tests/test_mode_env_validation.py -q</automated>
  </verify>
  <done>Mode-Konfigurationsmodule und Pipeline-Factory sind implementiert, Testfaelle fuer Moduswahl/ENV sind gruen und Typ-B-Default ist fest verdrahtet.</done>
</task>

</tasks>

<verification>
Fuehre `cd apps/agent && uv run pytest tests/test_mode_switch.py tests/test_mode_env_validation.py -q` aus. Pruefe zusaetzlich per `rg` in `apps/agent/src/model_factory.py`, dass kein impliziter Fallback auf Typ A enthalten ist (D-04), sowie per `rg "AGENT_MODE=type_b" apps/agent/.env.example`, dass der EU-Modus als Default dokumentiert ist (D-F).
</verification>

<success_criteria>
- AGNT-05 ist in der Codebasis vorbereitet durch getestete Typ-A/Typ-B-Factory bei gemeinsamem Runtime-Contract.
- Pflicht-ENV-Vertraege fuer beide Modi sind formal und testseitig abgesichert.
- Voice/Language bleiben kundenseitig konfigurierbar ohne Produkt-Hardcoding.
</success_criteria>

<output>
After completion, create `.planning/phases/05-eu-voice-agent-typ-b/05-01-SUMMARY.md`
</output>
