---
phase: 05-eu-voice-agent-typ-b
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/agent/agent.py
  - apps/agent/tests/test_mode_failure_policy.py
  - apps/agent/tests/test_agent.py
  - .planning/phases/05-eu-voice-agent-typ-b/05-VALIDATION.md
autonomous: true
requirements:
  - AGNT-05
must_haves:
  truths:
    - "Der Worker kann im selben Runtime-Image zwischen Typ A und Typ B umschalten."
    - "Das MCP-Toolset bleibt in beiden Modi identisch angebunden."
    - "Bei Typ-B-Providerfehlern endet die Session kontrolliert ohne stillen Moduswechsel."
    - "Technischer Smoke-Nachweis deckt DE und EN fuer Typ B ab."
  artifacts:
    - path: "apps/agent/agent.py"
      provides: "Entrypoint-Integration der Mode-Factory bei unveraenderter MCP-Wiring"
    - path: "apps/agent/tests/test_mode_failure_policy.py"
      provides: "Hard-Fail-Testabdeckung fuer D-03/D-04"
    - path: "apps/agent/tests/test_agent.py"
      provides: "Regression-Schutz fuer Session/MCP-Bootstrap"
    - path: ".planning/phases/05-eu-voice-agent-typ-b/05-VALIDATION.md"
      provides: "Aktualisierte Verify-Kommandos und Nyquist-konforme Wave-0-Referenzen"
  key_links:
    - from: "apps/agent/agent.py"
      to: "apps/agent/src/model_factory.py"
      via: "build_voice_pipeline(resolve_agent_mode())"
      pattern: "resolve_agent_mode\\(\\)|build_voice_pipeline\\("
    - from: "apps/agent/agent.py"
      to: "apps/agent/src/frappe_mcp.py"
      via: "MCPToolset(id='frappe_mcp', mcp_server=build_frappe_mcp_server())"
      pattern: "MCPToolset\\(id=\"frappe_mcp\""
---

<objective>
Verdrahte den bestehenden Agent-Entrypoint auf die neue Multi-Mode-Factory, sichere den Hard-Fail-Pfad fuer Typ B ab und finalisiere die technische Verifikation fuer AGNT-05.

Purpose: Der zweite Agent-Modus wird produktiv nutzbar, ohne MCP-Regressionen aus Phase 4 zu riskieren und ohne DSGVO-Policy-Verletzung durch Fallback.
Output: Entrypoint-Wiring, Failure-Policy-Tests und aktualisierte Validierungsstrategie fuer die Ausfuehrung.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-eu-voice-agent-typ-b/05-CONTEXT.md
@.planning/phases/05-eu-voice-agent-typ-b/05-RESEARCH.md
@.planning/phases/05-eu-voice-agent-typ-b/05-VALIDATION.md
@.planning/phases/05-eu-voice-agent-typ-b/05-01-PLAN.md
@apps/agent/agent.py
@apps/agent/src/mode_config.py
@apps/agent/src/model_factory.py
@apps/agent/src/frappe_mcp.py
@apps/agent/tests/test_agent.py

<interfaces>
From apps/agent/src/model_factory.py (from Plan 05-01):
```python
def build_voice_pipeline(mode: str) -> dict[str, object]
```

Required usage in agent entrypoint:
```python
mode = resolve_agent_mode()
validate_mode_env(mode)
pipeline = build_voice_pipeline(mode)
session = AgentSession(llm=pipeline["llm"], stt=pipeline.get("stt"), tts=pipeline.get("tts"), tools=[frappe_toolset], allow_interruptions=True)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entrypoint auf mode-basierte Pipeline umstellen, MCP unveraendert halten</name>
  <read_first>
    .planning/phases/05-eu-voice-agent-typ-b/05-CONTEXT.md
    apps/agent/agent.py
    apps/agent/src/frappe_mcp.py
    apps/agent/src/mode_config.py
    apps/agent/src/model_factory.py
    apps/agent/tests/test_agent.py
  </read_first>
  <files>apps/agent/agent.py, apps/agent/tests/test_agent.py</files>
  <acceptance_criteria>
    - `apps/agent/agent.py` nutzt `resolve_agent_mode()` + `validate_mode_env(mode)` vor Session-Start (D-06, D-F).
    - `agent_name` wird fuer Typ B auf `voice-eu` gesetzt bzw. geloggt (D-01) und nicht zur Fachrollensteuerung verwendet (D-02).
    - MCP-Wiring bleibt identisch: `build_frappe_mcp_server()` + `mcp.MCPToolset(id="frappe_mcp", ...)` in beiden Modi (Roadmap Success Criterion #2).
    - Keine Implementierung von Session-Switching oder Prompt-Provider-Runtime aus deferred ideas.
    - `cd apps/agent && uv run pytest tests/test_agent.py -q` endet mit Exit-Code 0.
  </acceptance_criteria>
  <action>Refaktoriere `agent.py` so, dass die bisher direkte OpenAI-Modellerzeugung durch mode-basiertes Factory-Wiring ersetzt wird: Mode aufloesen, ENV validieren, Pipeline laden und in `AgentSession` einsetzen. Setze fuer Typ B den Identitaetsraum `voice-eu` (D-01) und halte Rollenlogik weiterhin im Prompt (D-02). Belasse `build_frappe_mcp_server()` und `MCPToolset(id="frappe_mcp", ...)` unveraendert, damit das Toolset mode-agnostisch bleibt. Fuehre nur strukturelle Hooks ein; kein aktiver Runtime-Session-Switch und kein Prompt-Management-Featureausbau.</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_agent.py -q</automated>
  </verify>
  <done>Entrypoint nutzt die neue Factory fuer Typ A/B bei unveraendertem MCP-Vertrag und bestehende Agent-Basisregressionen bleiben gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hard-Fail-Policy und DE/EN-Smoke-Checks fuer Typ B absichern</name>
  <read_first>
    .planning/phases/05-eu-voice-agent-typ-b/05-CONTEXT.md
    .planning/phases/05-eu-voice-agent-typ-b/05-VALIDATION.md
    apps/agent/agent.py
    apps/agent/src/model_factory.py
    apps/agent/tests/test_mode_switch.py
    apps/agent/tests/test_mode_env_validation.py
  </read_first>
  <files>apps/agent/tests/test_mode_failure_policy.py, .planning/phases/05-eu-voice-agent-typ-b/05-VALIDATION.md</files>
  <acceptance_criteria>
    - `apps/agent/tests/test_mode_failure_policy.py` enthaelt `test_type_b_provider_error_triggers_hard_fail` (D-03).
    - `apps/agent/tests/test_mode_failure_policy.py` enthaelt Assertion, dass kein stiller Wechsel auf Typ A erfolgt (D-04).
    - `05-VALIDATION.md` referenziert konkrete Quick-Commands auf `uv run pytest` fuer `test_mode_switch.py`, `test_mode_env_validation.py`, `test_mode_failure_policy.py`.
    - `05-VALIDATION.md` bildet D-10 explizit mit DE+EN-Smoke-Nachweis als technische Verifikation ab (kein Produkt-Hoertest-Gate, D-09).
    - `cd apps/agent && uv run pytest tests/test_mode_switch.py tests/test_mode_env_validation.py tests/test_mode_failure_policy.py -q` endet mit Exit-Code 0.
  </acceptance_criteria>
  <action>Erstelle `tests/test_mode_failure_policy.py` mit simulierten Typ-B-Providerfehlern (z. B. beim Factory-Aufbau oder Session-Start) und pruefe, dass der Fehler sauber propagiert wird bzw. Session-Ende eintritt (D-03) und kein automatischer Pfad zu Typ A aktiviert wird (D-04). Aktualisiere `05-VALIDATION.md` auf den realen Testsatz aus Phase 5: konkrete `uv run pytest`-Kommandos pro Datei, Wave-0-Luecken geschlossen markieren, DE/EN-Smoke als technisches Muss dokumentieren (D-10) und den entfallenen Hoertest als bewusst ausgeschlossenes Produkt-Gate markieren (D-09).</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_mode_switch.py tests/test_mode_env_validation.py tests/test_mode_failure_policy.py -q</automated>
  </verify>
  <done>Hard-Fail ohne Fallback ist automatisiert abgesichert und die Phasenvalidierung bildet den finalen AGNT-05-Testvertrag nachvollziehbar ab.</done>
</task>

</tasks>

<verification>
Fuehre `cd apps/agent && uv run pytest tests/test_agent.py tests/test_mode_switch.py tests/test_mode_env_validation.py tests/test_mode_failure_policy.py -q` aus. Pruefe danach per `rg "voice-eu|AGENT_MODE|MCPToolset" apps/agent/agent.py`, dass Mode-Dispatch und MCP-Mode-Agnostik gemeinsam im Entrypoint verankert sind.
</verification>

<success_criteria>
- AGNT-05 ist end-to-end in Code + Tests fuer Typ-B-Default und Typ-A-Option abgebildet.
- MCP-Toolset bleibt zwischen Modis konsistent ohne Sonderpfade.
- D-03/D-04/D-09/D-10 sind technisch nachweisbar in Tests und Validation-Artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-eu-voice-agent-typ-b/05-02-SUMMARY.md`
</output>
