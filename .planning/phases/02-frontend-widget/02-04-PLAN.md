---
phase: 02-frontend-widget
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified: [components/widget/ChatPanel.tsx, components/widget/VoiceVisualizer.tsx]
autonomous: true
requirements: [WIDG-02]
must_haves:
  truths:
    - "Eine WebRTC-Verbindung wird nach Klick auf 'Gespräch starten' aufgebaut."
    - "Audio-Waveforms reagieren auf Spracheingaben (Visualisierung)."
    - "Status-Indikatoren zeigen Agenten-Aktivität (listening, speaking)."
  artifacts:
    - path: "components/widget/VoiceVisualizer.tsx"
      provides: "Audio-Visualisierung (Waveforms)"
    - path: "components/widget/ChatPanel.tsx"
      provides: "LiveKitRoom Integration und Status-Logik"
  key_links:
    - from: "components/widget/ChatPanel.tsx"
      to: "/api/token"
      via: "fetch"
      pattern: "fetch.*api/token"
    - from: "components/widget/VoiceVisualizer.tsx"
      to: "@livekit/components-react"
      via: "BarVisualizer/AgentAudioVisualizer"
      pattern: "Visualizer"
---

<objective>
Integration der LiveKit WebRTC Logik und Voice-Visualisierung in das Widget.

Purpose: Ermöglichen der eigentlichen Interaktion mit dem Agenten.
Output: Funktionierender Audio-Visualizer und Nachrichten-Stream.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-widget/02-RESEARCH.md
@.planning/phases/02-frontend-widget/02-02-SUMMARY.md
@.planning/phases/02-frontend-widget/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voice Visualizer Integration</name>
  <files>components/widget/VoiceVisualizer.tsx</files>
  <read_first>components/widget/ChatPanel.tsx</read_first>
  <action>
    Implementierung der Audio-Waveforms unter Verwendung von `AgentAudioVisualizerWave` aus `@livekit/components-react`.
    - Verbinde die Komponente mit dem aktiven Audio-Track des Agenten.
    - Styling entsprechend UI-SPEC (Accent Color `var(--widget-primary)`).
  </action>
  <verify>
    <automated>grep -q "AgentAudioVisualizerWave" components/widget/VoiceVisualizer.tsx || grep -q "AgentAudioVisualizerWave" components/widget/ChatPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - VoiceVisualizer.tsx existiert oder Komponente ist in ChatPanel integriert.
    - Nutze offizielle LiveKit Visualizer Komponenten.
  </acceptance_criteria>
  <done>Audio-Visualisierung ist in das Widget eingebettet.</done>
</task>

<task type="auto">
  <name>Task 2: Nachrichten-Historie und VAD Status</name>
  <files>components/widget/ChatPanel.tsx</files>
  <read_first>components/widget/ChatPanel.tsx</read_first>
  <action>
    Anzeige der Transkriptionen und des Agenten-Status (per D-03, D-04).
    - Nutze `useChat` und `useParticipantAttributes` Hooks von LiveKit.
    - Anzeige der Nachrichten in der `ScrollArea`.
    - Status-Labels laut UI-SPEC: "Agent hört zu...", "Agent denkt nach...", "Agent spricht...".
  </action>
  <verify>
    <automated>grep -q "Agent hört zu" components/widget/ChatPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ChatPanel enthält Logik zur Anzeige von Nachrichten.
    - Status-Indikatoren sind implementiert.
  </acceptance_criteria>
  <done>Chat-Historie und Status-Anzeigen sind funktionsfähig.</done>
</task>

<task type="auto">
  <name>Task 3: Verbindungs-Logik (Connect Flow)</name>
  <files>components/widget/ChatPanel.tsx</files>
  <read_first>components/LiveKitProvider.tsx</read_first>
  <action>
    Verdrahtung des "Gespräch starten" Buttons mit dem Token-Fetch und Room-Connect (per D-08).
    - Bei Klick: Fetch `/api/token`.
    - Initiale Verbindung zum LiveKit Room herstellen.
    - Voice-First Modus aktivieren (per D-09).
  </action>
  <verify>
    <automated>grep -q "fetch" components/widget/ChatPanel.tsx && grep -q "/api/token" components/widget/ChatPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Button triggert den Token-Abruf.
    - Erfolgreicher Connect führt zur Anzeige des Visualizers.
  </acceptance_criteria>
  <done>End-to-End Verbindungsaufbau ist möglich.</done>
</task>

</tasks>

<verification>
- Manueller Test: Button klicken, Verbindung muss hergestellt werden, Visualizer muss bei Sprache ausschlagen.
- Integration Test für den Verbindungs-Flow (Mocking der API).
</verification>

<success_criteria>
Das Widget kann eine echte WebRTC-Verbindung aufbauen und Interaktionen (Voice/Text) visualisieren.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-widget/02-04-SUMMARY.md`
</output>
