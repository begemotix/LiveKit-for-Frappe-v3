---
phase: 02-frontend-widget
plan: 06
type: execute
wave: 1
depends_on: ["02-05"]
files_modified: ["components/root-layout.tsx", "layout.tsx"]
autonomous: true
gap_closure: true
requirements: ["WIDG-01"]
must_haves:
  truths:
    - "The <html> tag in the rendered page contains inline styles for branding variables."
    - "Primary UI colors change when NEXT_PUBLIC_WIDGET_PRIMARY_COLOR is modified."
  artifacts:
    - path: "components/root-layout.tsx"
      provides: "Root layout with dynamic style injection"
  key_links:
    - from: "process.env"
      to: "components/root-layout.tsx"
      via: "inline style attribute on <html>"
---

<objective>
Close the gap identified in verification: Dynamic Branding Injection.
The goal is to ensure that environment variables like NEXT_PUBLIC_WIDGET_PRIMARY_COLOR correctly override the CSS variables in the UI, allowing for runtime branding changes without rebuilds.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-widget/02-VERIFICATION.md
@components/root-layout.tsx
@layout.tsx
@styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Branding Logic to RootLayout</name>
  <read_first>
    <file>components/root-layout.tsx</file>
    <file>layout.tsx</file>
  </read_first>
  <files>components/root-layout.tsx</files>
  <action>
    Modify `components/root-layout.tsx` to inject branding styles into the `<html>` tag.
    
    1. Extract the environment variables:
       - `NEXT_PUBLIC_WIDGET_PRIMARY_COLOR`
       - `NEXT_PUBLIC_WIDGET_PRIMARY_HOVER_COLOR`
       - `NEXT_PUBLIC_WIDGET_PRIMARY_COLOR_DARK`
       - `NEXT_PUBLIC_WIDGET_PRIMARY_HOVER_COLOR_DARK`
    
    2. Map these to CSS variables. To ensure they affect the shadcn/Tailwind theme, map them to:
       - `--primary`: `NEXT_PUBLIC_WIDGET_PRIMARY_COLOR`
       - `--primary-hover`: `NEXT_PUBLIC_WIDGET_PRIMARY_HOVER_COLOR` (if provided)
       - Also consider adding the `--widget-primary` variants if needed for specific components.
    
    3. Update the `RootLayout` component to accept these styles and apply them to the `<html>` tag's `style` attribute.
    
    Example implementation:
    ```typescript
    const brandingStyles = {
      '--primary': process.env.NEXT_PUBLIC_WIDGET_PRIMARY_COLOR,
      '--primary-hover': process.env.NEXT_PUBLIC_WIDGET_PRIMARY_HOVER_COLOR,
      // ... others
    } as React.CSSProperties;
    
    const cleanStyles = Object.fromEntries(
      Object.entries(brandingStyles).filter(([_, v]) => v !== undefined)
    );
    
    return (
      <html lang="en" style={cleanStyles} ...>
    ```
  </action>
  <verify>
    <automated>grep -q "style={cleanStyles}" components/root-layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>File `components/root-layout.tsx` contains logic to read `process.env.NEXT_PUBLIC_WIDGET_PRIMARY_COLOR`</criterion>
    <criterion>The `html` tag in `components/root-layout.tsx` has a `style` attribute receiving the branding variables</criterion>
  </acceptance_criteria>
  <done>RootLayout correctly injects environment-driven branding styles into the DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup and Final Verification</name>
  <read_first>
    <file>layout.tsx</file>
  </read_first>
  <files>layout.tsx</files>
  <action>
    Delete the orphaned `layout.tsx` file at the root directory to avoid confusion, as its logic has been migrated to `components/root-layout.tsx`.
  </action>
  <verify>
    <automated>ls layout.tsx 2>/dev/null || echo "deleted"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>File `layout.tsx` no longer exists at the root directory</criterion>
  </acceptance_criteria>
  <done>Project structure is cleaned up and only active layout code remains.</done>
</task>

</tasks>

<verification>
Check if setting `NEXT_PUBLIC_WIDGET_PRIMARY_COLOR` in `.env` (or mock it in a test) results in the `--primary` variable being present on the `<html>` element in the rendered output.
</verification>

<success_criteria>
1. `components/root-layout.tsx` handles dynamic style injection.
2. Unused root `layout.tsx` is removed.
3. Branding colors can be changed via environment variables without rebuilding the application (for development) or with a restart (for production).
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-widget/02-06-SUMMARY.md`
</output>
