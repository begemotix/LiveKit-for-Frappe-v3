---
phase: 02-frontend-widget
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  [
    components/widget/FloatingButton.tsx,
    components/widget/ChatPanel.tsx,
    components/widget/index.tsx,
  ]
autonomous: true
requirements: [WIDG-01]
must_haves:
  truths:
    - 'Das Widget ist als Floating Action Button (FAB) positioniert.'
    - 'Das Chat-Panel öffnet sich bei Klick auf den FAB.'
    - 'Das UI-Grundgerüst nutzt shadcn Komponenten (Card, Button).'
  artifacts:
    - path: 'components/widget/FloatingButton.tsx'
      provides: 'FAB-Icon und Toggle-Logic'
    - path: 'components/widget/ChatPanel.tsx'
      provides: 'Ausklappbares Chat-Interface'
  key_links:
    - from: 'components/widget/index.tsx'
      to: 'components/widget/FloatingButton.tsx'
      via: 'Import'
      pattern: 'FloatingButton'
    - from: 'components/widget/ChatPanel.tsx'
      to: 'components/ui/card.tsx'
      via: 'shadcn Card'
      pattern: 'Card'
---

<objective>
Bau der Widget-UI-Hülle bestehend aus dem Floating Action Button (FAB) und dem Chat-Panel.

Purpose: Bereitstellung der visuellen Schnittstelle für den Nutzer.
Output: Animierter FAB und ein ausklappbares Panel mit shadcn Komponenten.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-widget/02-UI-SPEC.md
@.planning/phases/02-frontend-widget/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Floating Action Button (FAB)</name>
  <files>components/widget/FloatingButton.tsx</files>
  <read_first>components/ui/button.tsx</read_first>
  <action>
    Implementierung des FAB unten rechts (per D-01) unter Nutzung des Templates als Orientierung.
    - Nutze `lucide-react` (MessageCircle Icon).
    - Fixed Positioning: `bottom-6 right-6`.
    - Toggle-State zum Öffnen des Panels.
  </action>
  <verify>
    <automated>grep -q "MessageCircle" components/widget/FloatingButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - FloatingButton.tsx existiert.
    - Button verwendet MessageCircle Icon.
    - Tailwind-Klassen für Positionierung vorhanden.
  </acceptance_criteria>
  <done>FAB ist implementiert und positioniert.</done>
</task>

<task type="auto">
  <name>Task 2: Chat Panel UI Shell</name>
  <files>components/widget/ChatPanel.tsx</files>
  <read_first>components/ui/card.tsx</read_first>
  <action>
    Erstellung des Chat-Panels unter Verwendung von shadcn `Card` und `ScrollArea` (per D-02).
    - Layout laut UI-SPEC (md: 16px Spacing, Heading: 18px).
    - Platzhalter für Voice-Visualizer und Nachrichten-Historie.
    - "Gespräch starten" Button als primärer CTA (per UI-SPEC Copywriting).
  </action>
  <verify>
    <automated>grep -q "Gespräch starten" components/widget/ChatPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ChatPanel.tsx existiert.
    - Nutze shadcn Card für den Container.
    - Enthält den Button mit Text "Gespräch starten".
  </acceptance_criteria>
  <done>Chat-Panel UI steht als Grundgerüst bereit.</done>
</task>

<task type="auto">
  <name>Task 3: Widget Integration</name>
  <files>components/widget/index.tsx, app/page.tsx</files>
  <read_first>app/page.tsx</read_first>
  <action>
    Zusammenführung von FAB und Panel in einer Hauptkomponente.
    - Handling des Open/Closed States.
    - Einbindung des Widgets in die Root-Seite `app/page.tsx` zum Testen (ersetzt/ergänzt den Template-Default).
  </action>
  <verify>
    <automated>grep -q "FloatingButton" components/widget/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - index.tsx exportiert das kombinierte Widget.
    - Das Widget wird in der Hauptseite gerendert.
  </acceptance_criteria>
  <done>Widget ist als integrierte Komponente auf der Seite sichtbar.</done>
</task>

</tasks>

<verification>
- `npm run dev` prüfen: FAB muss sichtbar sein und bei Klick das Panel öffnen.
- Unit Test für den Toggle-Mechanismus.
</verification>

<success_criteria>
Das Widget ist visuell vorhanden, positioniert und reagiert auf Nutzerinteraktion (Öffnen/Schließen).
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-widget/02-03-SUMMARY.md`
</output>
