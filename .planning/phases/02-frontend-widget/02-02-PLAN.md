---
phase: 02-frontend-widget
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [app/api/token/route.ts, components/LiveKitProvider.tsx]
autonomous: true
requirements: [WIDG-03, WIDG-02]
must_haves:
  truths:
    - 'Der API-Endpunkt /api/token generiert gültige LiveKit-Tokens.'
    - 'Tokens werden serverseitig erstellt (D-06).'
    - 'Der LiveKitRoom Provider ist im Frontend verfügbar.'
  artifacts:
    - path: 'app/api/token/route.ts'
      provides: 'Token-Generierung (Next.js Route Handler)'
    - path: 'components/LiveKitProvider.tsx'
      provides: 'Room Context Wrapper'
  key_links:
    - from: 'app/api/token/route.ts'
      to: 'livekit-server-sdk'
      via: 'AccessToken'
      pattern: 'AccessToken'
    - from: 'components/LiveKitProvider.tsx'
      to: '@livekit/components-react'
      via: 'LiveKitRoom'
      pattern: 'LiveKitRoom'
---

<objective>
Implementierung der serverseitigen Token-Generierung und des LiveKit Room Context Providers.

Purpose: Sicherstellung eines sicheren Verbindungsaufbaus ohne Preisgabe von Secrets.
Output: API-Endpunkt für Gast-Tokens und ein React-Wrapper für die Room-Logik.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-widget/02-RESEARCH.md
@.planning/phases/02-frontend-widget/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt Token API Route</name>
  <files>app/api/token/route.ts</files>
  <read_first>app/api/token/route.ts</read_first>
  <action>
    Anpassung der bestehenden API-Route im Template an unsere Anforderungen (D-06, D-07).
    - Überprüfung der Token-Generierung mit `AccessToken`.
    - Sicherstellen der Gast-Identität und der korrekten Room-Grants.
    - Falls Datei fehlt: Neu erstellen laut RESEARCH.md.
  </action>
  <verify>
    <automated>grep -q "AccessToken" app/api/token/route.ts && npx vitest run tests/api-token.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/api/token/route.ts existiert und generiert valide Tokens.
    - Die Test-Suite bestätigt die korrekte Funktion.
  </acceptance_criteria>
  <done>Token API ist angepasst und bereit.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt/Create LiveKit Provider</name>
  <files>components/LiveKitProvider.tsx</files>
  <read_first>app/layout.tsx</read_first>
  <action>
    Sicherstellen eines konsistenten `LiveKitProvider` für die App.
    - Falls vorhanden: Anpassen an unsere Props.
    - Falls fehlend: Erstellen einer Wrapper-Komponente um `LiveKitRoom`.
  </action>
  <verify>
    <automated>grep -q "LiveKitRoom" components/LiveKitProvider.tsx && npx vitest run tests/widget.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/LiveKitProvider.tsx stellt den Room-Kontext bereit.
  </acceptance_criteria>
  <done>Room Provider ist konfiguriert.</done>
</task>

</tasks>

<verification>
- Statische Code-Analyse mit grep.
- Ausführung von Vitest für Komponenten- und API-Logik.
</verification>

<success_criteria>
Tokens können sicher generiert werden und die Room-Infrastruktur steht im Frontend bereit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-widget/02-02-SUMMARY.md`
</output>
