---
phase: 02-frontend-widget
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/app/api/token/route.ts, src/components/LiveKitProvider.tsx]
autonomous: true
requirements: [WIDG-03, WIDG-02]
must_haves:
  truths:
    - "Der API-Endpunkt /api/token generiert gültige LiveKit-Tokens."
    - "Tokens werden serverseitig erstellt (D-06)."
    - "Der LiveKitRoom Provider ist im Frontend verfügbar."
  artifacts:
    - path: "src/app/api/token/route.ts"
      provides: "Token-Generierung (Next.js Route Handler)"
    - path: "src/components/LiveKitProvider.tsx"
      provides: "Room Context Wrapper"
  key_links:
    - from: "src/app/api/token/route.ts"
      to: "livekit-server-sdk"
      via: "AccessToken"
      pattern: "AccessToken"
    - from: "src/components/LiveKitProvider.tsx"
      to: "@livekit/components-react"
      via: "LiveKitRoom"
      pattern: "LiveKitRoom"
---

<objective>
Implementierung der serverseitigen Token-Generierung und des LiveKit Room Context Providers.

Purpose: Sicherstellung eines sicheren Verbindungsaufbaus ohne Preisgabe von Secrets.
Output: API-Endpunkt für Gast-Tokens und ein React-Wrapper für die Room-Logik.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-widget/02-RESEARCH.md
@.planning/phases/02-frontend-widget/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token API Route</name>
  <files>src/app/api/token/route.ts</files>
  <read_first>package.json</read_first>
  <action>
    Erstellung der API-Route zur Generierung von LiveKit-Tokens für anonyme Gäste (per D-06, D-07).
    - Nutze `AccessToken` aus `livekit-server-sdk`.
    - Identität: `guest-${Math.random().toString(36).substring(7)}`.
    - Grants: `roomJoin: true`, `room: process.env.LIVEKIT_DEFAULT_ROOM || 'lobby'`, `canPublish: true`, `canSubscribe: true`.
    - Rückgabe: `{ token: string }`.
  </action>
  <verify>
    <automated>grep -q "AccessToken" src/app/api/token/route.ts && npx vitest run tests/api-token.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Datei src/app/api/token/route.ts existiert.
    - Datei enthält einen POST Handler und nutzt das LiveKit SDK.
    - Der zugehörige Test `tests/api-token.test.ts` ist erfolgreich (nach Implementierung des Tests).
  </acceptance_criteria>
  <done>Token API ist implementiert und funktionsfähig.</done>
</task>

<task type="auto">
  <name>Task 2: LiveKit Room Provider</name>
  <files>src/components/LiveKitProvider.tsx</files>
  <read_first>src/app/layout.tsx</read_first>
  <action>
    Erstellung eines `LiveKitProvider` Komponenten-Wrappers, der den `LiveKitRoom` Context bereitstellt.
    - Verwendet `@livekit/components-react`.
    - Akzeptiert `token` und `serverUrl` als Props.
    - Verbindet sich mit dem LiveKit-Server aus `NEXT_PUBLIC_LIVEKIT_URL`.
  </action>
  <verify>
    <automated>grep -q "LiveKitRoom" src/components/LiveKitProvider.tsx && npx vitest run tests/widget.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/LiveKitProvider.tsx existiert und exportiert eine Komponente.
    - Komponente nutzt den `LiveKitRoom` Provider.
    - Der zugehörige Test `tests/widget.test.tsx` ist erfolgreich.
  </acceptance_criteria>
  <done>LiveKit Room Context ist für das Widget vorbereitet.</done>
</task>

</tasks>

<verification>
- Statische Code-Analyse mit grep.
- Ausführung von Vitest für Komponenten- und API-Logik.
</verification>

<success_criteria>
Tokens können sicher generiert werden und die Room-Infrastruktur steht im Frontend bereit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-widget/02-02-SUMMARY.md`
</output>
