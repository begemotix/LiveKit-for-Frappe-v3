---
phase: 02-frontend-widget
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, next.config.mjs, components.json, tailwind.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, tests/widget.test.tsx, tests/api-token.test.ts]
autonomous: true
requirements: [WIDG-01]
must_haves:
  truths:
    - "Das Next.js Projekt ist initialisiert und lauffähig."
    - "shadcn/ui ist konfiguriert und Basis-Komponenten sind vorhanden."
    - "Vitest ist für die Test-Automatisierung eingerichtet."
    - "Test-Stubs für die Validierung sind vorhanden."
  artifacts:
    - path: "package.json"
      provides: "Abhängigkeiten (Next.js, LiveKit, shadcn)"
    - path: "components.json"
      provides: "shadcn/ui Konfiguration"
    - path: "vitest.config.ts"
      provides: "Test-Konfiguration"
    - path: "tests/widget.test.tsx"
      provides: "Wave 0 Test-Stub für Widget"
    - path: "tests/api-token.test.ts"
      provides: "Wave 0 Test-Stub für API-Token"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "next|livekit-client"
---

<objective>
Initialisierung der Next.js Projektstruktur mit shadcn/ui und Installation der LiveKit SDKs.

Purpose: Schaffung einer soliden Basis für die Frontend-Entwicklung.
Output: Lauffähiges Next.js Projekt mit konfiguriertem UI-Framework und installierten Abhängigkeiten.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-widget/02-RESEARCH.md
@.planning/phases/02-frontend-widget/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Erstellung der Test-Stubs (Wave 0)</name>
  <files>tests/widget.test.tsx, tests/api-token.test.ts</files>
  <read_first>package.json</read_first>
  <action>
    Erstellung der Test-Stubs laut VALIDATION.md, um den Nyquist-Anforderungen gerecht zu werden.
    1. `mkdir tests` falls nicht vorhanden.
    2. Erstellung von `tests/widget.test.tsx` mit einem einfachen Platzhalter-Test.
    3. Erstellung von `tests/api-token.test.ts` mit einem einfachen Platzhalter-Test.
  </action>
  <verify>
    <automated>test -f tests/widget.test.tsx && test -f tests/api-token.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test-Dateien existieren im tests/ Verzeichnis.
  </acceptance_criteria>
  <done>Wave 0 Test-Stubs sind erstellt.</done>
</task>

<task type="auto">
  <name>Task 1: Next.js und LiveKit SDK Installation</name>
  <files>package.json, tsconfig.json, next.config.mjs</files>
  <read_first>package.json</read_first>
  <action>
    Initialisierung des Next.js Projekts und Installation der Kern-Abhängigkeiten.
    1. Backup der bestehenden package.json (falls nötig).
    2. Ausführen von: `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes`.
    3. Installation der LiveKit SDKs: `npm install livekit-client livekit-server-sdk @livekit/components-react @livekit/components-styles lucide-react clsx tailwind-merge`.
  </action>
  <verify>
    <automated>grep -q "next" package.json && grep -q "livekit-client" package.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json enthält "next", "livekit-client" und "@livekit/components-react".
    - Verzeichnis src/app existiert.
  </acceptance_criteria>
  <done>Next.js und LiveKit Abhängigkeiten sind installiert.</done>
</task>

<task type="auto">
  <name>Task 2: shadcn/ui Initialisierung und Komponenten</name>
  <files>components.json, tailwind.config.ts, src/app/globals.css, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/scroll-area.tsx, src/components/ui/avatar.tsx</files>
  <read_first>package.json, tailwind.config.ts, src/app/globals.css</read_first>
  <action>
    Initialisierung von shadcn/ui und Hinzufügen der benötigten Komponenten laut UI-SPEC.
    1. Ausführen von: `npx shadcn@latest init --yes`.
    2. Installation der Komponenten: `npx shadcn@latest add button card scroll-area avatar --yes`.
  </action>
  <verify>
    <automated>test -f components.json && test -f src/components/ui/button.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components.json wurde erstellt.
    - Komponenten button, card, scroll-area und avatar befinden sich in src/components/ui/.
  </acceptance_criteria>
  <done>shadcn/ui ist konfiguriert und Basis-Komponenten sind vorhanden.</done>
</task>

<task type="auto">
  <name>Task 3: Test-Infrastruktur Setup (Vitest) & Wave 0 Stubs</name>
  <files>vitest.config.ts, package.json, tests/widget.test.tsx, tests/api-token.test.ts</files>
  <read_first>package.json</read_first>
  <action>
    Einrichtung von Vitest und Erstellung der Wave 0 Test-Stubs laut VALIDATION.md.
    1. Installation: `npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom`.
    2. Erstellung der `vitest.config.ts` mit React-Plugin und JSDOM-Umgebung.
    3. Hinzufügen des "test" Scripts in die package.json: `"test": "vitest run"`.
    4. `mkdir tests` falls nicht vorhanden.
    5. Erstellung von `tests/widget.test.tsx` und `tests/api-token.test.ts` mit Platzhalter-Tests (Nyquist Wave 0).
  </action>
  <verify>
    <automated>grep -q "vitest" package.json && test -f vitest.config.ts && test -f tests/widget.test.tsx && test -f tests/api-token.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts existiert.
    - package.json enthält vitest Abhängigkeiten und das test Script.
  </acceptance_criteria>
  <done>Test-Infrastruktur ist bereit für die Entwicklung.</done>
</task>

</tasks>

<verification>
- `npm run dev` startet ohne Fehler.
- `npm test` führt (leere) Tests aus.
</verification>

<success_criteria>
Next.js Projekt ist vollständig aufgesetzt, shadcn Komponenten sind installiert und Vitest ist konfiguriert.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-widget/02-01-SUMMARY.md`
</output>
