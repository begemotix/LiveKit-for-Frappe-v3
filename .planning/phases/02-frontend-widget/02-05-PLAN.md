---
phase: 02-frontend-widget
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified: [src/styles/globals.css, src/app/layout.tsx, tailwind.config.ts]
autonomous: true
requirements: [WIDG-01, WIDG-02, WIDG-03]
must_haves:
  truths:
    - "Farben und Branding sind über Umgebungsvariablen steuerbar (D-05)."
    - "Das Widget ist vollständig responsiv und UI-SPEC konform."
    - "Alle automatisierten Tests der Phase sind erfolgreich."
  artifacts:
    - path: "src/styles/globals.css"
      provides: "CSS Variablen Definitionen"
    - path: "tailwind.config.ts"
      provides: "Tailwind Integration der CSS Variablen"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/styles/globals.css"
      via: "Inline Style Injection"
      pattern: "--widget-primary"
---

<objective>
Finalisierung des Brandings über dynamische CSS-Variablen und Abschlussprüfung der Phase.

Purpose: Gewährleistung von White-Labeling-Fähigkeit und hoher Qualität.
Output: Dynamisch gestaltetes Widget und vollständiger Validierungsbericht.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-widget/02-UI-SPEC.md
@.planning/phases/02-frontend-widget/02-VALIDATION.md
@.planning/phases/02-frontend-widget/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamisches Styling (CSS Variablen)</name>
  <files>src/styles/globals.css, tailwind.config.ts, src/app/layout.tsx</files>
  <read_first>src/styles/globals.css</read_first>
  <action>
    Implementierung des dynamischen Stylings basierend auf Umgebungsvariablen (per D-05).
    - Definiere CSS Variablen in `:root` (z.B. `--widget-primary`).
    - Lade Werte aus `process.env.NEXT_PUBLIC_WIDGET_PRIMARY_COLOR` etc. in der Root Layout Komponente und setze sie als Inline-Styles.
    - Update `tailwind.config.ts`, um diese Variablen zu verwenden (z.B. `primary: "var(--widget-primary)"`).
  </action>
  <verify>
    <automated>grep -q "\-\-widget-primary" src/styles/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - globals.css enthält die Variablen-Definitionen.
    - tailwind.config.ts referenziert die Variablen.
    - Layout übergibt Umgebungsvariablen an das CSS.
  </acceptance_criteria>
  <done>Das Widget-Branding ist über Umgebungsvariablen steuerbar.</done>
</task>

<task type="auto">
  <name>Task 2: Abschließende Validierung und Phase Sign-Off</name>
  <files>02-VERIFICATION.md</files>
  <read_first>.planning/phases/02-frontend-widget/02-VALIDATION.md</read_first>
  <action>
    Durchführung aller automatisierten und manuellen Tests laut VALIDATION.md.
    1. `npm test` für die gesamte Suite.
    2. Manueller Check der UI-Interaktionen und Audio-Feedback.
    3. Erstellung des `02-VERIFICATION.md` Berichts.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Alle Tests sind grün.
    - 02-VERIFICATION.md ist erstellt und dokumentiert den Erfolg.
  </acceptance_criteria>
  <done>Phase 02 ist erfolgreich validiert und abgeschlossen.</done>
</task>

</tasks>

<verification>
- `npm test` muss erfolgreich durchlaufen.
- Visueller Abgleich mit UI-SPEC.
</verification>

<success_criteria>
Das Frontend-Widget ist voll funktionsfähig, gebrandet und erfolgreich gegen alle Anforderungen geprüft.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-widget/02-05-SUMMARY.md`
</output>
