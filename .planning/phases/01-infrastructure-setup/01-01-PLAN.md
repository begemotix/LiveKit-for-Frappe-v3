---
phase: 01-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ['.env.example', 'livekit.yaml']
autonomous: true
requirements: [INFR-03, INFR-04]
must_haves:
  truths:
    - 'Deployment can be customized via .env without code changes'
    - 'The server supports WebRTC connections and integrated TURN via configuration'
  artifacts:
    - path: '.env.example'
      provides: 'Generic environment configuration template'
      contains: 'LIVEKIT_API_KEY'
    - path: 'livekit.yaml'
      provides: 'LiveKit server configuration'
      contains: 'turn:'
  key_links:
    - from: 'livekit.yaml'
      to: '.env.example'
      via: 'Environment variables mapping in yaml'
      pattern: "\\${LIVEKIT_API_KEY}"
---

<objective>
Create the base LiveKit configuration and environment structure.

Purpose: To ensure the infrastructure can be generically customized via environment variables for white-labeling, networking, and API keys without code changes, and to set up the single-node LiveKit configuration with integrated TURN.
Output: `.env.example` template and `livekit.yaml` server configuration file.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-setup/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Environment Variables</name>
  <files>.env.example</files>
  <read_first>
    - .env.example
  </read_first>
  <action>
    Create `.env.example` with concrete configuration variables.
    Define LiveKit variables: `LIVEKIT_API_KEY=devkey`, `LIVEKIT_API_SECRET=devsecret`.
    Define Network variables: `DOMAIN=livekit.example.com`, `LETSENCRYPT_EMAIL=admin@example.com`.
    Define White-Labeling per INFR-03: `WIDGET_PRIMARY_COLOR=#000000`, `WIDGET_BACKGROUND_COLOR=#ffffff`.
    Define TURN/STUN external IP variable: `EXTERNAL_IP=your_public_ipv4`.
    Include commented-out Coolify/Traefik examples (per D-04): `# TRAEFIK_ROUTER_NAME=livekit`.
  </action>
  <verify>
    <automated>grep -q "LIVEKIT_API_KEY" .env.example && grep -q "WIDGET_PRIMARY_COLOR" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `LIVEKIT_API_KEY=`
    - `.env.example` contains `LIVEKIT_API_SECRET=`
    - `.env.example` contains `DOMAIN=`
    - `.env.example` contains `WIDGET_PRIMARY_COLOR=`
    - `.env.example` contains `EXTERNAL_IP=`
  </acceptance_criteria>
  <done>Environment variable template is fully populated with all required variables.</done>
</task>

<task type="auto">
  <name>Task 2: Create LiveKit Server Configuration</name>
  <files>livekit.yaml</files>
  <read_first>
    - livekit.yaml
    - .env.example
  </read_first>
  <action>
    Use the `user-livekit-docs` MCP to look up the optimal `livekit.yaml` configuration for a single-node setup (per D-01) with an integrated TURN server (per D-05).
    Create `livekit.yaml` with concrete values mapping to the `.env.example` variables:
    - Set `port: 7880`, `bind: "0.0.0.0"`.
    - Set `rtc: tcp_port: 7881`, `udp_port_min: 50000`, `udp_port_max: 60000`, `use_external_ip: true`.
    - Set `turn: enabled: true`, `domain: "${DOMAIN}"`, `tls_port: 5349`, `udp_port: 3478`, `external_tls: true`.
    - Set `keys:` section to use environment variables (e.g. `${LIVEKIT_API_KEY}: "${LIVEKIT_API_SECRET}"`).
    Ensure the configuration does NOT use Redis.
  </action>
  <verify>
    <automated>grep -q "turn:" livekit.yaml && grep -q "tcp_port: 7881" livekit.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `livekit.yaml` contains `port: 7880`
    - `livekit.yaml` contains `turn:` with `enabled: true`
    - `livekit.yaml` contains `udp_port_min` and `udp_port_max` under `rtc:`
    - `livekit.yaml` contains `keys:` section mapping the API key and secret
    - `livekit.yaml` does NOT contain `redis:` section
  </acceptance_criteria>
  <done>LiveKit configuration supports integrated TURN without Redis and loads keys from ENV.</done>
</task>

</tasks>

<verification>
Check if `.env.example` and `livekit.yaml` have been created and contain the specified variables and configurations.
</verification>

<success_criteria>

- `.env.example` provides all parameters needed for white-labeling, networking, and keys.
- `livekit.yaml` provides a single-node, Redis-less configuration with integrated TURN enabled.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-setup/01-01-SUMMARY.md`
</output>
