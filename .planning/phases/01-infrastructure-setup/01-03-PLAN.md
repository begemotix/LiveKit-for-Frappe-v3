---
phase: 01-infrastructure-setup
plan: 03
type: gap-closure
status: ready
date: 2026-04-19
depends_on: ["01-02"]
files_modified:
  - .planning/phases/01-infrastructure-setup/01-CONTEXT.md
  - .planning/PROJECT.md
  - .planning/phases/01-infrastructure-setup/OPERATOR-HANDOVER.md
  - .planning/STATE.md
objective: "Realign Phase 1 documentation with deployed production stack."
must_haves:
  - "Keine ENV-Variable im deployten Stack bleibt ohne Erklärung in OPERATOR-HANDOVER.md."
---

# Phase 01-03 — Gap Closure Plan

## Objective
Realign Phase 1 documentation with deployed production stack.

## Scope
Nur Dokumentation wird angepasst. Es gibt keine Code-Änderungen.

## Tasks

1. **Bestandsaufnahme der Infrastruktur-Drift seit `02-06`**
   - `43d65b6` — docs(coolify): explain fixed ports vs env and Coolify caveats.
   - `c4b9d5a` — feat(deploy): multi-tenant host ports via render-compose; Caddy opt-in profile.
   - `49869c7` — feat(deploy): switch to `network_mode: host` and reduced UDP range.
   - `53f81b2` — fix(deploy): rename livekit config filename to avoid Coolify collision.
   - `7aa5cbb` — fix(deploy): rename container-side config path to avoid directory collision.
   - `46cb552` — fix(deploy): use `--config-body` in docker-compose for Coolify compatibility.
   - `3636727` — fix(deploy): remove invalid `bind` field from livekit config-body.
   - `92783f5` — feat(deploy): unify stack into multi-container deployment.
   - `03ad0f5` — fix(compose): literal UDP publish and Coolify override example.
   - `1930d60` — feat(deploy): compose host ports per env for parallel stacks.
   - `45eac35` — fix(frontend): remove `network_mode: host` for Traefik SSL routing.
   - `5840f64` — refactor(infra): move stack to bridge networking for universal SSL support.
   - `b1098b0` — refactor(infra): switch to hybrid host/bridge networking.
   - `a5c5ee4` — refactor(infra): harden hybrid wired stack for SSL stability.
   - `14d86d3` — fix(infra): split internal/external LiveKit URLs for browser connectivity.
   - `d009e6a` — refactor(infra): replace hardcoded IPs with `host.docker.internal`.
   - `bbb1f38` — docs: finalize Coolify config with verified `host.docker.internal` wiring.

2. **Dokumentationsangleichung**
   - Update `.planning/phases/01-infrastructure-setup/01-CONTEXT.md` mit realen Networking-Entscheidungen:
     - `host.docker.internal` als universelles internes Wiring
     - Hybrid bridge/host networking
     - Traefik-Labels im Produktivpfad statt Caddy-Default
   - Update `.planning/PROJECT.md` mit denselben produktiven Entscheidungen als akzeptierte Architektur-Realität.

3. **Operator-Handover für Phase 1**
   - Erzeuge `.planning/phases/01-infrastructure-setup/OPERATOR-HANDOVER.md` gemäß Operator-Handover-Pflicht.
   - Dokumentiere alle relevanten Coolify-Variablen aus `.env.example` plus produktionsrelevante Proxy-/Netzwerkvariablen.
   - Stelle sicher, dass keine deployte ENV ohne Erklärung bleibt (must-have).

4. **State-Realignment**
   - Ergänze `.planning/STATE.md`, dass Phase 1 weiterhin complete ist und Plan `01-03` den realen Stack formalisiert.
   - Ergänze Decision-Eintrag zum realen Produktivstack (Coolify-Traefik + `host.docker.internal`-Wiring).

## Verification

- Alle geänderten Dateien liegen unter `.planning/` (Doku-only).
- Phase-1-Dokumentation beschreibt den tatsächlich deployten Stack ohne Widerspruch.
- `OPERATOR-HANDOVER.md` enthält mindestens:
  - Coolify-ENV-Variablen
  - Konfigurierbare Komponenten
  - Nicht-konfigurierbare Komponenten
  - Handover-Checkliste Onboarding

## Output

- `.planning/phases/01-infrastructure-setup/01-03-PLAN.md`
- `.planning/phases/01-infrastructure-setup/OPERATOR-HANDOVER.md`
- Aktualisierte `.planning/phases/01-infrastructure-setup/01-CONTEXT.md`
- Aktualisierte `.planning/PROJECT.md`
- Aktualisierte `.planning/STATE.md`
